"""
Каталог уроков курса (lesson_id → контент + чеклист для ИИ).
52 урока: Введение (2) · HTML (12) · CSS (18) · JavaScript (20) — от новичка до Junior.
"""

from typing import Any, Dict, List

LESSON_ORDER: List[str] = [
    # ── MODULE 0: Введение (2 урока) ─────────────────────────────────────────
    "M0-L1",  "M0-L2",
    # ── MODULE 1: HTML (12 уроков) ──────────────────────────────────────────
    "M1-L1",  "M1-L2",  "M1-L3",  "M1-L4",  "M1-L5",  "M1-L6",
    "M1-L7",  "M1-L8",  "M1-L9",  "M1-L10", "M1-L11", "M1-L12",
    # ── MODULE 2: CSS (18 уроков) ───────────────────────────────────────────
    "M2-L1",  "M2-L2",  "M2-L3",  "M2-L4",  "M2-L5",  "M2-L6",
    "M2-L7",  "M2-L8",  "M2-L9",  "M2-L10", "M2-L11", "M2-L12",
    "M2-L13", "M2-L14", "M2-L15", "M2-L16", "M2-L17", "M2-L18",
    # ── MODULE 3: JavaScript (20 уроков) ────────────────────────────────────
    "M3-L1",  "M3-L2",  "M3-L3",  "M3-L4",  "M3-L5",  "M3-L6",
    "M3-L7",  "M3-L8",  "M3-L9",  "M3-L10", "M3-L11", "M3-L12",
    "M3-L13", "M3-L14", "M3-L15", "M3-L16", "M3-L17", "M3-L18",
    "M3-L19", "M3-L20",
]

LESSONS: Dict[str, Dict[str, Any]] = {

    # ════════════════════════════════════════════════════════════════════════
    # MODULE 0 · ВВЕДЕНИЕ · 2 урока
    # ════════════════════════════════════════════════════════════════════════

    "M0-L1": {
        "title": "Добро пожаловать на курс",
        "progress_badge": "Введение · 1/2",
        "opening": (
            "👋 Привет! Ты попал на бесплатный курс по веб-разработке.\n"
            "Этот урок объяснит: что ты будешь учить, как всё устроено и что тебя ждёт в конце."
        ),
        "hook": "~3 мин: прочитай — и сразу будет понятно что делать дальше.",
        "theory": (
            "🎓 <b>Что это за курс?</b>\n\n"
            "Бесплатный курс Frontend-разработки от нуля до Junior-уровня.\n"
            "Frontend — это то, что пользователь <b>видит в браузере</b>: кнопки, тексты, картинки, анимации.\n\n"
            "К концу курса ты умеешь:\n"
            "  ✅ Верстать страницы на <b>HTML</b> (структура)\n"
            "  ✅ Оформлять их на <b>CSS</b> (внешний вид)\n"
            "  ✅ Добавлять интерактивность на <b>JavaScript</b> (поведение)\n\n"
            "━━━━━━━━━━━━━━━━━━━━\n"
            "📚 <b>Программа — 52 урока:</b>\n\n"
            "  📌 Модуль 0 — Введение (2 урока) — ты здесь\n"
            "  📌 Модуль 1 — HTML (12 уроков)\n"
            "  📌 Модуль 2 — CSS (18 уроков)\n"
            "  📌 Модуль 3 — JavaScript (20 уроков)\n\n"
            "━━━━━━━━━━━━━━━━━━━━\n"
            "⚙️ <b>Как устроен каждый урок:</b>\n\n"
            "1️⃣ <b>Читай теорию</b> — объяснение концепции с примерами.\n\n"
            "2️⃣ <b>Выполни задание</b> — напиши код сам, не копируй. Только так формируется навык.\n\n"
            "3️⃣ <b>Закрой урок одним из двух способов:</b>\n"
            "   • <b>Отправь код</b> — ИИ-ментор проверит и даст фидбек.\n"
            "   • <b>Пройди быстрый тест</b> — 2 вопроса по теории урока.\n\n"
            "4️⃣ <b>Следующий урок</b> открывается автоматически после закрытия текущего.\n\n"
            "━━━━━━━━━━━━━━━━━━━━\n"
            "👤 <b>Кто создал курс?</b>\n\n"
            "Вадим — веб-разработчик из Беларуси, 7+ лет опыта.\n"
            "Стек: HTML/CSS, JavaScript, Python, Telegram-боты.\n"
            "Сайт: <a href=\"https://vadzim.by/pro-menya/\">vadzim.by</a>"
        ),
        "nuance": (
            "Если застрял — не сиди в тишине. Напиши вопрос боту прямо здесь, вставь код с проблемой. "
            "Оптимальный темп: 1 урок в день. 52 урока — это ~2 месяца до Junior-уровня."
        ),
        "task": (
            "Напиши боту одно сообщение: своё имя и что хочешь создать после курса.\n"
            "Например: «Меня зовут Алекс, хочу сделать портфолио-сайт».\n"
            "Это не проверяется ИИ — просто полезно сформулировать цель для себя."
        ),
        "checklist": [
            "Ознакомился с программой курса (52 урока)",
            "Понимает как закрыть урок (код или квиз)",
            "Знает имя автора курса — Вадим",
        ],
        "hint": "Следующий урок откроется автоматически после того как ты пройдёшь квиз или отправишь код.",
        "quiz_done_line": (
            "Курс и механика в голове — пройди быстрый тест и идём дальше."
        ),
        "win_message": (
            "🚀 Отлично! Ты знаешь что тебя ждёт и как всё работает.\n"
            "Дальше — как устроен интернет и браузер: фундамент для всего что будем делать."
        ),
                "quiz": [
            {
                "q": 'Сколько уроков в этом курсе?',
                "options": [
                    '12',
                    '30',
                    '52',
                    '100',
                ],
                "correct": 2,
                "wrong_hint": '52 урока: Введение (2) + HTML (12) + CSS (18) + JavaScript (20).',
            },
            {
                "q": 'Как закрыть урок и открыть следующий?',
                "options": [
                    'Написать /next',
                    'Подождать 24 часа',
                    'Отправить код на проверку или пройти быстрый тест',
                    'Нажать кнопку «Следующий урок»',
                ],
                "correct": 2,
                "wrong_hint": 'Урок закрывается только двумя способами: код на проверку или квиз. Тогда открывается следующий.',
            },
        ],
    },

    "M0-L2": {
        "title": "Как устроен интернет и браузер",
        "progress_badge": "Введение · 2/2",
        "opening": (
            "❓ Что происходит между нажатием Enter в адресной строке и появлением страницы?\n"
            "👉 Цепочка из DNS, TCP, HTTP и рендеринга браузера. Всё это занимает миллисекунды."
        ),
        "hook": "~6 мин: поймёшь как работает интернет и браузер — фундамент для всего, что будем делать.",
        "theory": (
            "🌐 <b>Как работает интернет</b>\n\n"
            "<b>1. DNS — телефонная книга интернета</b>\n"
            "Когда ты вводишь <code>google.com</code>, браузер спрашивает DNS-сервер: "
            "«какой IP-адрес у этого домена?». DNS отвечает: <code>142.250.185.46</code>. "
            "Теперь браузер знает куда стучаться.\n\n"
            "<b>2. TCP/IP — как данные добираются</b>\n"
            "Данные в интернете идут пакетами (как письма). "
            "TCP гарантирует, что все пакеты дошли и в правильном порядке. "
            "IP — адресация: знает маршрут от тебя до сервера.\n\n"
            "<b>3. HTTP/HTTPS — язык общения браузера и сервера</b>\n"
            "Браузер отправляет <b>запрос</b> (Request): «дай мне страницу /index.html».\n"
            "Сервер отвечает <b>ответом</b> (Response): код состояния + содержимое.\n"
            "Коды: <code>200 OK</code>, <code>404 Not Found</code>, <code>500 Server Error</code>.\n"
            "HTTPS = HTTP + шифрование (TLS). Замочек в браузере — это оно.\n\n"
            "━━━━━━━━━━━━━━━━━━━━\n"
            "🖥 <b>Как работает браузер</b>\n\n"
            "<b>Шаги от HTML до картинки на экране:</b>\n\n"
            "1. <b>Парсинг HTML</b> → строится DOM (Document Object Model) — дерево тегов.\n"
            "2. <b>Парсинг CSS</b> → строится CSSOM — дерево стилей.\n"
            "3. <b>Render Tree</b> = DOM + CSSOM — что рисовать и как.\n"
            "4. <b>Layout</b> — вычисляет размеры и позиции элементов.\n"
            "5. <b>Paint</b> — рисует пиксели.\n"
            "6. <b>Composite</b> — склеивает слои (GPU).\n\n"
            "JavaScript выполняется <b>движком</b> (V8 в Chrome). "
            "JS однопоточный — только одна задача одновременно. "
            "Долгие операции (запросы к серверу) выполняются асинхронно.\n\n"
            "━━━━━━━━━━━━━━━━━━━━\n"
            "🎨 <b>Что такое Frontend</b>\n\n"
            "Frontend — всё, что пользователь <b>видит и с чем взаимодействует</b> в браузере.\n"
            "Backend — сервер: хранит данные, обрабатывает логику, возвращает ответы.\n\n"
            "Стек Frontend-разработчика:\n"
            "  • <b>HTML</b> — структура (скелет страницы).\n"
            "  • <b>CSS</b> — стили (внешний вид).\n"
            "  • <b>JavaScript</b> — поведение (интерактивность).\n\n"
            "Это как дом: HTML — стены и перекрытия, CSS — ремонт и дизайн, JS — электрика и сантехника."
        ),
        "nuance": (
            "DevTools (F12 в браузере) — главный инструмент фронтенд-разработчика. "
            "Вкладки Elements (DOM), Console (JS), Network (запросы), Sources (код). "
            "Открывай его при каждом уроке — это твой отладчик."
        ),
        "task": (
            "Открой любой сайт в браузере (например vadzim.by). "
            "Нажми F12 (DevTools). Изучи вкладки:\n"
            "• <b>Elements</b> — найди тег &lt;body&gt; и разверни его.\n"
            "• <b>Network</b> — перезагрузи страницу (F5), посмотри список запросов.\n"
            "• <b>Console</b> — напиши: <code>document.title</code> и нажми Enter. Что вернулось?\n"
            "Ответ напиши боту."
        ),
        "checklist": [
            "Открыл DevTools (F12) на любом сайте",
            "Нашёл тег body в Elements",
            "Видел запросы во вкладке Network",
            "Ввёл document.title в Console",
        ],
        "hint": "F12 или правая кнопка мыши → «Просмотр кода элемента» — оба способа открывают DevTools.",
        "quiz_done_line": (
            "Интернет и браузер — не магия, а понятная цепочка. Открой DevTools и потыкай. Потом зачёт."
        ),
        "win_message": (
            "🌐 Теперь ты знаешь, что происходит за кулисами каждой страницы.\n"
            "<b>Ты уже понимаешь:</b> DNS, HTTP, DOM, рендеринг и роль фронтенда.\n"
            "Введение пройдено! Следующий шаг — HTML: начинаем строить страницы."
        ),
                "quiz": [
            {
                "q": 'Пользователь открыл URL. Правильный порядок?',
                "options": [
                    'JS → CSS → HTML → отрисовка',
                    'HTML → DOM → CSSOM → Render Tree → отрисовка',
                    'CSS → HTML → JS → отрисовка',
                    'DNS → JS → HTML → CSS',
                ],
                "correct": 1,
                "wrong_hint": 'Браузер: HTML → DOM, CSS → CSSOM, объединяет в Render Tree, рисует пиксели.',
            },
            {
                "q": 'Сервер вернул код 404. Что это означает?',
                "options": [
                    'Запрос выполнен успешно',
                    'Страница или файл не найден',
                    'Ошибка на сервере',
                    'Соединение установлено',
                ],
                "correct": 1,
                "wrong_hint": '404 Not Found — запрошенная страница не существует. 200 — успех, 500 — ошибка сервера.',
            },
        ],
    },

    # ════════════════════════════════════════════════════════════════════════
    # MODULE 1 · HTML · 12 уроков
    # ════════════════════════════════════════════════════════════════════════

    "M1-L1": {
        "title": "Кнопка &lt;button&gt;",
        "progress_badge": "HTML · 1/12",
        "opening": (
            "❓ Где на экране появится кнопка, если ты её написал в HTML?\n"
            "👉 Там, куда ты её положил в <code>&lt;body&gt;</code> — не в head."
        ),
        "hook": "~3 мин: сделаешь свою кликабельную кнопку и поймёшь <code>type</code> — без форм и паники.",
        "theory": (
            "<code>&lt;button&gt;Текст&lt;/button&gt;</code> — то, что люди жмут. Текст внутри виден.\n"
            "Ставят в меню, формах, карточках — везде, где нужен явный клик.\n\n"
            "Браузер рисует кнопку сам: серый прямоугольник с рамкой по умолчанию. "
            "Потом CSS сделает её красивой — сейчас важно, что она <b>работает</b>."
        ),
        "nuance": (
            "Внутри <code>&lt;form&gt;</code> кнопка по умолчанию отправляет форму. "
            "Не хочешь этого — пиши <code>type=\"button\"</code>. "
            "Хочешь отправить — <code>type=\"submit\"</code>. Хочешь сбросить — <code>type=\"reset\"</code>."
        ),
        "task": (
            "В <code>&lt;body&gt;</code> — одна кнопка «Нажми» с <code>type=\"button\"</code>. "
            "Открой файл в браузере: кнопка должна быть видна."
        ),
        "checklist": [
            "Есть открывающий и закрывающий тег button",
            "Внутри есть видимый текст (например «Нажми»)",
            "Указан type=\"button\"",
        ],
        "hint": "Минимум: html, body, одна строка button внутри body.",
        "quiz_done_line": (
            "Уже схватил: видимое — в body, «просто клик» без формы — <code>type=\"button\"</code>. "
            "Осталось вставить в файл и отправить код на проверку или пройти быстрый тест."
        ),
        "win_message": (
            "🔥 Ты только что собрал настоящий кликабельный элемент. Это уже кусок интерфейса.\n"
            "<b>Ты уже умеешь:</b> вывести кнопку и не дать ей ломать форму зря.\n"
            "Дальше — ссылки: страница начнёт вести куда-то ещё."
        ),
                "quiz": [
            {
                "q": 'Кнопка без атрибута type внутри <form>. Что произойдёт при нажатии?',
                "options": [
                    'Ничего — тип обязателен',
                    "Форма отправится (type='submit' по умолчанию)",
                    'Страница перезагрузится без отправки',
                    'Браузер запросит подтверждение',
                ],
                "correct": 1,
                "wrong_hint": "Без type у button внутри form — браузер считает его type='submit'.",
            },
            {
                "q": 'type="reset" у кнопки внутри формы — что произойдёт при нажатии?',
                "options": [
                    'Форма отправится на сервер',
                    'Страница перезагрузится',
                    'Поля формы вернутся к исходным значениям',
                    'Кнопка исчезнет',
                ],
                "correct": 2,
                "wrong_hint": 'type="reset" — сбрасывает поля формы. type="submit" — отправляет. type="button" — ничего сам не делает.',
            },
        ],
    },

    "M1-L2": {
        "title": "Ссылка &lt;a&gt;",
        "progress_badge": "HTML · 2/12",
        "opening": (
            "❓ Ссылка и кнопка — одно и то же?\n"
            "👉 Нет: ссылка ведёт по URL; кнопка ждёт действия. Сейчас — только ссылки."
        ),
        "hook": "~3 мин: две ссылки — и ты уже умеешь меню «как у людей» (текст ясный, вторая вкладка — безопасно).",
        "theory": (
            "<code>&lt;a href=\"URL\"&gt;текст&lt;/a&gt;</code> — переход по адресу. "
            "Текст ссылки должен говорить <b>куда ведёт</b>, а не «нажми сюда» — это важно для поиска и для людей со скринридерами.\n\n"
            "Ссылка может вести на:\n"
            "  • Внешний сайт: <code>href=\"https://example.com\"</code>\n"
            "  • Якорь на этой же странице: <code>href=\"#section\"</code> — браузер прокрутит к элементу с <code>id=\"section\"</code>\n"
            "  • Другой файл проекта: <code>href=\"about.html\"</code>\n"
            "  • Скачивание файла: добавь атрибут <code>download</code>\n\n"
            "<b>Абсолютный путь</b> (<code>href=\"/about\"</code>) — всегда от корня сайта, независимо от текущей страницы.\n"
            "<b>Относительный путь</b> (<code>href=\"about.html\"</code>) — от текущей папки."
        ),
        "nuance": (
            "Новая вкладка: <code>target=\"_blank\"</code> + <code>rel=\"noopener noreferrer\"</code> — "
            "привычный безопасный набор. Без rel=\"noopener\" открытая вкладка могла получить доступ к родительской — дыра безопасности."
        ),
        "task": (
            "Две ссылки в body: (1) на любой живой URL — текст нормальный, не «сюда»; "
            "(2) в новой вкладке с <code>target=\"_blank\"</code> и <code>rel=\"noopener noreferrer\"</code>."
        ),
        "checklist": [
            "Два тега a с href",
            "Первая ссылка с понятным текстом",
            "Вторая с target=\"_blank\" и rel=\"noopener noreferrer\"",
        ],
        "hint": "Кавычки в href, не забудь закрыть &lt;/a&gt;.",
        "quiz_done_line": (
            "Понял про новую вкладку и rel — значит, не ловишься на типичной дыре. "
            "Вшей это в разметку и отправь код на проверку или пройди быстрый тест."
        ),
        "win_message": (
            "🚀 Ссылки с нормальным текстом и безопасная новая вкладка — это уже логика навигации.\n"
            "<b>Ты уже умеешь:</b> вести пользователя по URL как взрослый.\n"
            "Дальше картинка: страница станет не только кликабельной, но и визуальной."
        ),
                "quiz": [
            {
                "q": "href='/about' и href='about' — в чём разница?",
                "options": [
                    'Нет разницы — оба ведут на /about',
                    '/about — от корня сайта; about — относительно текущей страницы',
                    'Оба работают только в HTML5',
                    'href без / — это ошибка валидации',
                ],
                "correct": 1,
                "wrong_hint": 'Абсолютный путь /about всегда от корня; относительный about — от текущего URL.',
            },
            {
                "q": "<a href='#contacts'>Контакты</a> — что произойдёт при клике?",
                "options": [
                    'Откроется страница contacts',
                    "Страница прокрутится к элементу с id='contacts'",
                    'Ошибка — одиночный # не работает',
                    'Страница обновится с пустым хэшем',
                ],
                "correct": 1,
                "wrong_hint": "# — якорная ссылка. Браузер найдёт элемент с id='contacts' и прокрутит к нему.",
            },
        ],
    },

    "M1-L3": {
        "title": "Картинка &lt;img&gt;",
        "progress_badge": "HTML · 3/12",
        "opening": (
            "❓ Картинка без alt — кому больнее всего?\n"
            "👉 Тем, кто не видит экран: смысл пропадает. Одна фраза в alt — и ты молодец."
        ),
        "hook": "~3 мин: вставишь картинку по URL и подпишешь её смысл — страница «оживёт».",
        "theory": (
            "<code>&lt;img src=\"url\" alt=\"текст\"&gt;</code> — одиночный тег, без закрытия. "
            "src — адрес картинки (URL или путь к файлу). "
            "alt — что на картинке по смыслу; если изображение не загрузилось — виден он.\n\n"
            "Размер можно задать атрибутами <code>width</code> и <code>height</code> (в пикселях) или через CSS. "
            "Лучше задавать хотя бы один из них: браузер зарезервирует место под картинку, "
            "страница не будет «прыгать» пока грузится.\n\n"
            "<b>Форматы изображений:</b>\n"
            "  • <b>JPG</b> — фотографии. Хорошее сжатие, нет прозрачности.\n"
            "  • <b>PNG</b> — логотипы, иконки, скриншоты. Поддерживает прозрачность.\n"
            "  • <b>SVG</b> — векторная графика. Масштабируется без потери качества.\n"
            "  • <b>WebP</b> — современный формат. Лучше JPG по сжатию, поддерживает прозрачность."
        ),
        "nuance": (
            "Пустой alt (<code>alt=\"\"</code>) допустим для чисто декоративных изображений — "
            "скринридер тогда пропустит его. В этом задании — осмысленный alt."
        ),
        "task": (
            "Один <code>img</code> с рабочим URL (например <code>https://picsum.photos/200</code>) "
            "и осмысленным <code>alt</code> по-русски. Открой в браузере."
        ),
        "checklist": [
            "Есть тег img с src (URL)",
            "Есть alt с описанием, не пустой для этого задания",
        ],
        "hint": "src: https://picsum.photos/200 — alt одной короткой фразой, что на фото.",
        "quiz_done_line": (
            "Суть alt в голове — дальше только вставить в файл и проверить в браузере. "
            "Отправь код на проверку или пройди быстрый тест."
        ),
        "win_message": (
            "🎯 Картинка + осмысленный alt — ты думаешь не только «красиво», а «понятно».\n"
            "<b>Ты уже умеешь:</b> вставлять визуал с подписью по делу.\n"
            "Дальше — списки: мысли перестанут расползаться простынёй текста."
        ),
                "quiz": [
            {
                "q": "Картинка не загрузилась. Написано: <img src='photo.jpg' alt='Портрет'>. Что увидит пользователь?",
                "options": [
                    'Пустое белое место',
                    'Красный крест',
                    'Текст «Портрет»',
                    'Заглушку браузера с иконкой',
                ],
                "correct": 2,
                "wrong_hint": 'alt отображается вместо картинки когда та не загрузилась — и для скринридеров.',
            },
            {
                "q": 'Для логотипа с прозрачным фоном лучший формат?',
                "options": [
                    'JPG — хорошее сжатие, подходит для всего',
                    'PNG — поддерживает прозрачность',
                    'BMP — без сжатия, самый чёткий',
                    'TXT — текстовый формат',
                ],
                "correct": 1,
                "wrong_hint": 'JPG не поддерживает прозрачность. Для логотипа с прозрачным фоном — PNG или SVG.',
            },
        ],
    },

    "M1-L4": {
        "title": "Списки ul и ol",
        "progress_badge": "HTML · 4/12",
        "opening": (
            "❓ Порядок важен в списке покупок?\n"
            "👉 Да — нумерация; нет — маркеры. В HTML под это есть разные теги."
        ),
        "hook": "~3 мин: маркированный и нумерованный список — страница сразу «дышит», особенно с телефона.",
        "theory": (
            "<code>&lt;ul&gt;&lt;li&gt;…&lt;/li&gt;&lt;/ul&gt;</code> — неупорядоченный (маркеры), порядок не главное.\n"
            "<code>&lt;ol&gt;&lt;li&gt;…&lt;/li&gt;&lt;/ol&gt;</code> — упорядоченный, шаги, рейтинг, очерёдность.\n\n"
            "Списки — основа навигации, карточек, меню. Любое меню сайта в HTML — это <code>ul li a</code>. "
            "Можно вкладывать один список в другой (вложенные списки) — главное не уходить в пять уровней."
        ),
        "nuance": "Каждый пункт — только внутри <code>&lt;li&gt;</code>, не клади текст прямо внутрь ul или ol.",
        "task": (
            "В body: один <code>ol</code> из 3 пунктов (например шаги «как я учусь») и один <code>ul</code> из 3 пунктов (хобби). "
            "Открой в браузере."
        ),
        "checklist": [
            "Есть ol с тремя li",
            "Есть ul с тремя li",
            "Теги закрыты корректно",
        ],
        "hint": "Сначала открой ol или ul, внутри только li, потом закрой список.",
        "quiz_done_line": (
            "Поймал разницу ul/ol — осталось набить в файл и глянуть в браузере. "
            "Потом отправь код на проверку или пройди быстрый тест."
        ),
        "win_message": (
            "📋 Списки — суперсила против простыней текста.\n"
            "<b>Ты уже умеешь:</b> структурировать мысли так, что их хочется листать.\n"
            "Дальше — таблица: когда данные реально в строках и колонках."
        ),
                "quiz": [
            {
                "q": "Что выведет: <ol start='3'><li>Пункт</li></ol>?",
                "options": [
                    '• Пункт',
                    '1. Пункт',
                    '3. Пункт',
                    'Пункт (без нумерации)',
                ],
                "correct": 2,
                "wrong_hint": "Атрибут start задаёт начальный номер. start='3' → нумерация с 3.",
            },
            {
                "q": 'Можно ли вложить <ul> внутрь <li>?',
                "options": [
                    'Нет — это невалидный HTML',
                    'Да — вложенные списки это стандартный HTML',
                    'Только если задать display: block',
                    'Только для ol, не для ul',
                ],
                "correct": 1,
                "wrong_hint": 'Вложенный список внутри <li> — стандарт. Так делают многоуровневые меню.',
            },
        ],
    },

    "M1-L5": {
        "title": "Простая таблица",
        "progress_badge": "HTML · 5/12",
        "opening": (
            "❓ Выровнять фильмы и годы абзацами — удобно?\n"
            "👉 Редко. Таблица — когда колонки реально про разные поля одной строки."
        ),
        "hook": "~4 мин: одна маленькая таблица — и ты уже читаешь данные «как в Excel», но в браузере.",
        "theory": (
            "<code>table</code> → строки <code>tr</code> → ячейки <code>th</code> (заголовок) и <code>td</code> (данные). "
            "Минимум: шапка из th, дальше строки из td.\n\n"
            "По умолчанию таблица без границ и красоты — всё добавляет CSS. "
            "Атрибут <code>border=\"1\"</code> добавляет рамку прямо в HTML (старый способ, только для быстрого теста). "
            "Элементы <code>thead</code>, <code>tbody</code>, <code>tfoot</code> — семантические обёртки для частей таблицы."
        ),
        "nuance": "Таблицу не используют для вёрстки (это было в 2000-е) — только когда сетка колонок осмысленна.",
        "task": (
            "Таблица 2×3: первая строка — два <code>th</code> (например «Фильм» и «Год»); "
            "две следующие строки — по два <code>td</code> с выдуманными данными. Открой в браузере."
        ),
        "checklist": [
            "Есть table, tr, th, td",
            "Первая строка с th",
            "Минимум две строки данных с td",
        ],
        "hint": "Структура: table > tr > th+th, потом tr > td+td, ещё tr > td+td.",
        "quiz_done_line": (
            "Сетка в голове есть — вбей разметку и проверь отображение. "
            "Дальше только практика и кнопки внизу."
        ),
        "win_message": (
            "📊 Таблица без магии — чистая разметка.\n"
            "<b>Ты уже умеешь:</b> показать строки с колонками так, что не стыдно показать ментору.\n"
            "Дальше — семантика: страница начнёт «знать», где шапка, где сердце, где подвал."
        ),
                "quiz": [
            {
                "q": "Что делает colspan='3' у ячейки таблицы?",
                "options": [
                    'Увеличивает высоту ячейки на 3 строки',
                    'Ячейка занимает 3 колонки по горизонтали',
                    'Добавляет 3px отступа внутри ячейки',
                    'Объединяет 3 строки вертикально',
                ],
                "correct": 1,
                "wrong_hint": 'colspan — горизонтальное объединение колонок. rowspan — вертикальное.',
            },
            {
                "q": 'Зачем <thead> и <tbody>, если можно просто <tr>?',
                "options": [
                    'Они обязательны — без них таблица не работает',
                    'Только для красивого кода',
                    'Семантика: скринридеры и браузеры понимают структуру; tbody можно прокручивать отдельно',
                    'thead добавляет автоматический жирный шрифт',
                ],
                "correct": 2,
                "wrong_hint": 'thead/tbody/tfoot — семантическое разделение, доступность и возможность независимой прокрутки тела.',
            },
        ],
    },

    "M1-L6": {
        "title": "Семантика: header, main, footer",
        "progress_badge": "HTML · 6/12",
        "opening": (
            "❓ Зачем теги вроде header, если есть div?\n"
            "👉 Чтобы браузер, поиск и люди понимали: где ввод, где основной контент, где подвал."
        ),
        "hook": "~4 мин: скелет «как у сайта» — header, main, footer; без дизайна, но со смыслом.",
        "theory": (
            "<code>header</code> — вводная зона (лого, заголовок, навигация).\n"
            "<code>main</code> — главное содержимое страницы, обычно один на странице.\n"
            "<code>footer</code> — подвал, мелкий текст, ссылки, копирайт.\n"
            "<code>section</code> — тематический раздел с заголовком.\n"
            "<code>article</code> — самостоятельный блок (пост, новость, карточка).\n"
            "<code>aside</code> — боковой контент, дополнительный к основному.\n\n"
            "Поисковики и скринридеры понимают эти теги: правильная семантика — это SEO и доступность в подарок."
        ),
        "nuance": "<code>nav</code> кладут там, где навигация по сайту — хотя бы список ссылок внутри.",
        "task": (
            "В body: <code>header</code> с одним <code>h1</code>, внутри <code>nav</code> с <code>ul</code> из 2 ссылок; "
            "ниже <code>main</code> с одним <code>p</code>; в конце <code>footer</code> с коротким текстом «© я». "
            "Открой в браузере."
        ),
        "checklist": [
            "Есть header с h1",
            "Внутри header есть nav с ul и двумя a",
            "Есть main с p",
            "Есть footer с текстом",
        ],
        "hint": "Порядок сверху вниз: header → main → footer; nav внутри header.",
        "quiz_done_line": (
            "Понял, зачем дублировать смысл тегами, а не кучей div — встрой в файл и проверь. "
            "Потом зачёт."
        ),
        "win_message": (
            "🏗 Страница с header/main/footer — уже не «просто текст», а каркас сайта.\n"
            "<b>Ты уже умеешь:</b> говорить браузеру, где что по смыслу.\n"
            "Дальше — формы: пользователь сможет что-то ввести."
        ),
                "quiz": [
            {
                "q": 'Зачем <article> вместо <div> для поста в блоге?',
                "options": [
                    'article загружается быстрее',
                    'div нельзя стилизовать через CSS',
                    'Семантика: поисковики и скринридеры понимают что это самостоятельный контент',
                    'article автоматически добавляет тень',
                ],
                "correct": 2,
                "wrong_hint": 'Семантические теги помогают SEO и доступности. article — самодостаточный блок контента.',
            },
            {
                "q": 'Где семантически правильно разместить копирайт и ссылку на политику?',
                "options": [
                    'header',
                    'nav',
                    'aside',
                    'footer',
                ],
                "correct": 3,
                "wrong_hint": 'footer — подвал страницы: копирайт, контакты, вторичные ссылки.',
            },
        ],
    },

    "M1-L7": {
        "title": "Форма: label и input",
        "progress_badge": "HTML · 7/12",
        "opening": (
            "❓ Плейсхолдер заменяет подпись к полю?\n"
            "👉 Нет. Плейсхолдер исчезает — label остаётся для всех, включая скринридеры."
        ),
        "hook": "~4 мин: одно поле ввода с нормальной подписью — уже форма, с которой не стыдно в прод.",
        "theory": (
            "<code>&lt;form&gt;</code> — контейнер для полей.\n"
            "<code>&lt;label for=\"id\"&gt;</code> + <code>&lt;input id=\"…\" type=\"text\"&gt;</code> — "
            "клик по подписи фокусирует поле, если <code>for</code> и <code>id</code> совпадают.\n\n"
            "Типы input: <code>text</code>, <code>email</code>, <code>password</code>, <code>number</code>, "
            "<code>checkbox</code>, <code>radio</code>, <code>file</code>, <code>date</code> — у каждого своя клавиатура на мобиле и базовая валидация.\n"
            "Атрибут <code>required</code> не даёт отправить форму с пустым полем — без единой строки JS."
        ),
        "nuance": "Пока без отправки на сервер — можно без <code>action</code>; главное — связка label + input.",
        "task": (
            "Форма: один <code>label for</code> с текстом «Имя» и рядом <code>input type=\"text\"</code> с тем же <code>id</code>. "
            "Открой в браузере — клик по «Имя» должен активировать поле."
        ),
        "checklist": [
            "Есть form",
            "label с for совпадает с id у input",
            "input type=\"text\"",
        ],
        "hint": "Сделай id=\"name\" и for=\"name\" — самый простой рабочий пример.",
        "quiz_done_line": (
            "Связка label/input в голове — осталось вставить в HTML и потыкать в браузере. "
            "Дальше «✅» или код."
        ),
        "win_message": (
            "✍️ Подписанное поле — база любых форм.\n"
            "<b>Ты уже умеешь:</b> сделать ввод, с которым не борются ни пальцы, ни доступность.\n"
            "Дальше — многострочный ввод и кнопка отправки."
        ),
                "quiz": [
            {
                "q": "<label>Email</label><input type='email'> — связаны ли они через for/id?",
                "options": [
                    'Да — они рядом в HTML, этого достаточно',
                    'Нет — нужны совпадающие for у label и id у input',
                    'Да — тип email связывает их автоматически',
                    'Только если добавить aria-labelledby',
                ],
                "correct": 1,
                "wrong_hint": 'Без совпадения for/id — клик по label не активирует поле. Это ошибка доступности.',
            },
            {
                "q": "<input type='number' min='1' max='100'>. Введёт ли пользователь 200?",
                "options": [
                    'Нет — браузер заблокирует ввод',
                    'Да — min/max не блокируют ввод, только валидируют при отправке формы',
                    'Зависит от браузера',
                    'Да, но поле окрасится красным сразу',
                ],
                "correct": 1,
                "wrong_hint": 'min/max — подсказки для браузера и валидация при submit, но ввод не блокируется.',
            },
        ],
    },

    "M1-L8": {
        "title": "textarea и отправка",
        "progress_badge": "HTML · 8/12",
        "opening": (
            "❓ Длинный текст в одну строку input — удобно?\n"
            "👉 Нет. Для абзаца — textarea; для «отправить» — submit (когда форма реально шлётся)."
        ),
        "hook": "~4 мин: поле для сообщения + кнопка отправки — как в мини-обратной связи.",
        "theory": (
            "<code>&lt;textarea&gt;&lt;/textarea&gt;</code> — много строк, можно задать <code>rows</code> и <code>cols</code>.\n"
            "<code>&lt;button type=\"submit\"&gt;</code> или <code>input type=\"submit\"</code> — отправляет форму.\n\n"
            "У формы есть атрибуты: <code>action</code> (куда шлём), <code>method</code> (GET/POST). "
            "Без <code>action</code> браузер перезагрузит текущую страницу — это нормально для учебных примеров. "
            "Атрибут <code>placeholder</code> у textarea показывает подсказку, пока поле пустое."
        ),
        "nuance": "Локально форма может просто перезагрузить страницу — для учебы важно увидеть, что кнопка живая.",
        "task": (
            "Внутри <code>form</code>: <code>label</code> + <code>textarea</code> с id (for на label), "
            "ниже <code>button type=\"submit\"</code> с текстом «Отправить». Открой в браузере, нажми кнопку."
        ),
        "checklist": [
            "Есть form",
            "textarea с связанным label (for/id)",
            "Кнопка type=\"submit\"",
        ],
        "hint": "textarea не одиночный — открывающий и закрывающий тег.",
        "quiz_done_line": (
            "Разница textarea vs input на пальцах — вшей в разметку и нажми submit в браузере. "
            "Потом зачёт урока."
        ),
        "win_message": (
            "🚀 Форма с текстом и submit — ты собрал микро-диалог с пользователем.\n"
            "<b>Ты уже умеешь:</b> принять больше одной строки и дать явную «отправить».\n"
            "Дальше — скелет документа: как устроен файл .html снаружи."
        ),
                "quiz": [
            {
                "q": "Форма с <input type='password'>. Данные отправляются зашифрованными?",
                "options": [
                    "Да — type='password' шифрует данные при отправке",
                    "Нет — type='password' только скрывает символы; шифрование обеспечивает HTTPS",
                    'Зависит от браузера',
                    'Да, если добавить атрибут secure',
                ],
                "correct": 1,
                "wrong_hint": 'Password маскирует ввод визуально. Безопасная передача — задача HTTPS, не HTML.',
            },
            {
                "q": "<button type='submit'> вне тега <form>. Отправит форму?",
                "options": [
                    'Да, всегда',
                    'Нет — submit работает только внутри form (или при указании атрибута form=id)',
                    'Только в Chrome',
                    'Только если есть action у формы',
                ],
                "correct": 1,
                "wrong_hint": "submit без родительской form ничего не отправит. Можно связать через form='form-id'.",
            },
        ],
    },

    "M1-L9": {
        "title": "Скелет HTML: DOCTYPE, head, meta",
        "progress_badge": "HTML · 9/12",
        "opening": (
            "❓ Почему на некоторых сайтах кракозябры вместо текста?\n"
            "👉 Забыли указать кодировку. Одна строка meta — и проблема исчезает."
        ),
        "hook": "~4 мин: напишешь правильный HTML-шаблон с нуля — тот, с которого стартует каждый настоящий проект.",
        "theory": (
            "Любой HTML-файл начинается с объявления типа документа и структуры:\n\n"
            "<code>&lt;!DOCTYPE html&gt;</code> — говорит браузеру: «это современный HTML5, не старый».\n"
            "<code>&lt;html lang=\"ru\"&gt;</code> — корень документа; <code>lang</code> нужен для скринридеров и SEO.\n"
            "<code>&lt;head&gt;</code> — невидимая зона с метаданными:\n"
            "  • <code>&lt;meta charset=\"utf-8\"&gt;</code> — кодировка, всегда первой строкой в head.\n"
            "  • <code>&lt;meta name=\"viewport\" content=\"width=device-width, initial-scale=1\"&gt;</code> — адаптивность на мобиле.\n"
            "  • <code>&lt;title&gt;Моя страница&lt;/title&gt;</code> — заголовок вкладки браузера.\n"
            "  • <code>&lt;link rel=\"stylesheet\" href=\"style.css\"&gt;</code> — подключение CSS.\n"
            "<code>&lt;body&gt;</code> — всё видимое.\n\n"
            "Это <b>шаблон</b>, который используется в каждом новом HTML-файле."
        ),
        "nuance": (
            "<code>&lt;meta name=\"viewport\"&gt;</code> без него сайт на мобиле выглядит как уменьшенный десктоп. "
            "Это обязательная строка для любого современного сайта."
        ),
        "task": (
            "Создай файл с полным скелетом: DOCTYPE, html с lang, head с charset + viewport + title, body с одним h1 «Привет, мир!». "
            "Открой в браузере — во вкладке должен быть твой title."
        ),
        "checklist": [
            "Есть DOCTYPE html",
            "html с атрибутом lang",
            "meta charset utf-8",
            "meta viewport",
            "title с текстом",
            "body с h1",
        ],
        "hint": "Скопируй шаблон из теории и замени заголовок — это стандартный старт.",
        "quiz_done_line": (
            "Шаблон в голове — вбей в файл, открой вкладку и проверь title. "
            "Потом отправь код на проверку или пройди быстрый тест."
        ),
        "win_message": (
            "🗂 Теперь у тебя есть правильный стартовый шаблон — не слепленный наугад, а осмысленный.\n"
            "<b>Ты уже умеешь:</b> начать любой HTML-проект с правильной основы.\n"
            "Дальше — заголовки и абзацы: текстовая иерархия страницы."
        ),
                "quiz": [
            {
                "q": "Что произойдёт если убрать <meta charset='UTF-8'> со страницы с кириллицей?",
                "options": [
                    'Ничего — charset необязателен',
                    'Страница не загрузится',
                    'Текст может отображаться кракозябрами',
                    'CSS не применится',
                ],
                "correct": 2,
                "wrong_hint": 'Без charset браузер угадывает кодировку — и часто ошибается. Кириллица превращается в «кракозябры».',
            },
            {
                "q": 'Мета-тег viewport: что случится на мобиле без него?',
                "options": [
                    'Страница отобразится корректно',
                    'Браузер масштабирует страницу как десктопную — текст становится мелким',
                    'CSS не применится к мобиле',
                    'Изображения не загрузятся',
                ],
                "correct": 1,
                "wrong_hint": 'Без viewport мобильный браузер рендерит страницу как 980px и уменьшает — всё крошечное.',
            },
        ],
    },

    "M1-L10": {
        "title": "Заголовки h1–h6 и абзацы",
        "progress_badge": "HTML · 10/12",
        "opening": (
            "❓ Зачем шесть уровней заголовков — не хватило бы двух?\n"
            "👉 Поисковик и читатель ориентируются по иерархии: h1 — тема, h2 — раздел, h3 — подраздел."
        ),
        "hook": "~3 мин: выстроишь текстовую иерархию — страница станет понятной без всякого дизайна.",
        "theory": (
            "Заголовки <code>h1</code>–<code>h6</code> — от самого важного к наименее важному.\n"
            "Правила хорошего тона:\n"
            "  • <b>h1 — один на странице</b>, главная тема.\n"
            "  • h2 — основные разделы, h3 — подразделы внутри них.\n"
            "  • Не пропускай уровни: не прыгай с h1 сразу на h4.\n\n"
            "Абзац — <code>&lt;p&gt;</code>. Пустая строка между двумя <code>p</code> — два абзаца. "
            "Перенос строки внутри текста — <code>&lt;br&gt;</code> (одиночный тег, без закрытия).\n\n"
            "Выделение: <code>&lt;strong&gt;</code> — важное (жирный), <code>&lt;em&gt;</code> — акцент (курсив). "
            "Оба несут семысл, в отличие от просто <code>bold/italic</code> через CSS."
        ),
        "nuance": (
            "Не используй h2 только потому что «хочется большой шрифт» — для этого есть CSS font-size. "
            "Заголовки несут смысловую иерархию, а не только стиль."
        ),
        "task": (
            "Страница: h1 с названием, два h2 — раздела, под каждым h2 — по одному p с текстом из 2–3 предложений. "
            "В одном абзаце выдели слово через strong, в другом — через em."
        ),
        "checklist": [
            "Один h1",
            "Два h2",
            "Два p с текстом",
            "Есть strong и em",
        ],
        "hint": "Придумай любую тему — например «Мой день»: утро и вечер.",
        "quiz_done_line": (
            "Иерархия заголовков в голове — набей в файл, глянь в браузере. Потом зачёт."
        ),
        "win_message": (
            "📝 Структурированный текст — половина дизайна без единой строчки CSS.\n"
            "<b>Ты уже умеешь:</b> выстраивать иерархию так, как это делают в реальных проектах.\n"
            "Дальше — div, span и атрибуты class/id: инструменты для точечного CSS."
        ),
                "quiz": [
            {
                "q": 'В чём разница между <em> и <i>?',
                "options": [
                    'Нет разницы — оба делают курсив',
                    '<em> — смысловой акцент (интонация); <i> — просто курсивный шрифт',
                    '<i> — устаревший тег, не использовать',
                    '<em> работает только внутри параграфов',
                ],
                "correct": 1,
                "wrong_hint": '<em> несёт смысл (скринридер делает акцент); <i> — только визуальный стиль (иностранные слова, термины).',
            },
            {
                "q": 'На странице: <h1>Заголовок</h1><h3>Подзаголовок</h3> (h2 пропущен). Это проблема?',
                "options": [
                    'Нет — заголовки нумеруются произвольно',
                    'Да — нарушает иерархию, плохо для SEO и скринридеров',
                    'Только если нет CSS',
                    'Только если h3 жирный',
                ],
                "correct": 1,
                "wrong_hint": 'Иерархия заголовков h1→h2→h3 важна для семантики, доступности и поисковиков.',
            },
        ],
    },

    "M1-L11": {
        "title": "div, span, class и id",
        "progress_badge": "HTML · 11/12",
        "opening": (
            "❓ Зачем div, если есть header, main, section?\n"
            "👉 div — универсальная обёртка без смысла. Нет подходящего семантического тега — бери div."
        ),
        "hook": "~4 мин: научишься группировать элементы и вешать классы — это основа любой вёрстки.",
        "theory": (
            "<code>&lt;div&gt;</code> — блочный контейнер (занимает всю ширину, начинается с новой строки).\n"
            "<code>&lt;span&gt;</code> — строчный контейнер (не переносит строку, обёртка внутри текста).\n\n"
            "Атрибут <code>class</code> — имя для группы элементов. Один элемент может иметь несколько классов: "
            "<code>class=\"card featured\"</code>. CSS и JS обращаются к классу через точку: <code>.card</code>.\n\n"
            "Атрибут <code>id</code> — уникальный идентификатор, один раз на странице. "
            "CSS обращается через решётку: <code>#header</code>. Используй id для якорных ссылок и JS-хуков.\n\n"
            "Практика: <code>&lt;div class=\"card\"&gt;...&lt;/div&gt;</code> — карточка товара. "
            "CSS потом говорит <code>.card { border: 1px solid; }</code> — и все карточки получают рамку."
        ),
        "nuance": (
            "id должен быть уникальным на странице. class — нет. "
            "В CSS id имеет более высокую специфичность, чем class — это создаёт проблемы. "
            "Поэтому в современных проектах id в CSS почти не используют — только class."
        ),
        "task": (
            "Два div с одинаковым class=\"card\", внутри каждого — h3 и p. "
            "Один span с class=\"highlight\" внутри одного из p. "
            "Один из div — с уникальным id."
        ),
        "checklist": [
            "Два div с class=\"card\"",
            "Внутри каждого div есть h3 и p",
            "Есть span с классом",
            "Один div с id",
        ],
        "hint": "Придумай карточки «Фильм 1» и «Фильм 2» — самый простой пример.",
        "quiz_done_line": (
            "div/span и class/id понятны — вбей в файл. Потом кнопки внизу."
        ),
        "win_message": (
            "🧩 div + class — строительные блоки любого интерфейса.\n"
            "<b>Ты уже умеешь:</b> группировать элементы для CSS-оформления.\n"
            "Дальше — доступность: aria и title. Потом стартуем CSS."
        ),
                "quiz": [
            {
                "q": "Два элемента с id='submit'. Что вернёт document.getElementById('submit')?",
                "options": [
                    'Массив из двух элементов',
                    'Только первый — это баг, id должен быть уникальным',
                    'Браузер выдаст ошибку',
                    'Случайный из двух',
                ],
                "correct": 1,
                "wrong_hint": 'getElementById возвращает первый найденный. Дублирование id — нарушение стандарта и источник багов.',
            },
            {
                "q": 'Стратегическая разница между class и id?',
                "options": [
                    'Только в синтаксисе (. и #)',
                    'class — для группы похожих элементов; id — для уникального, используется как якорь',
                    'id работает только в CSS, class — в JS',
                    'class нельзя использовать в JavaScript',
                ],
                "correct": 1,
                "wrong_hint": 'class — многоразовый (много элементов); id — уникальный (один на страницу), якорь для навигации и JS.',
            },
        ],
    },

    "M1-L12": {
        "title": "Доступность: title, aria-label, tabindex",
        "progress_badge": "HTML · 12/12",
        "opening": (
            "❓ Кто не может пользоваться твоим сайтом прямо сейчас?\n"
            "👉 Люди со скринридерами — если ты не добавил пару атрибутов. Это 15% пользователей."
        ),
        "hook": "~4 мин: три атрибута, которые делают интерфейс понятным для всех — без лишнего кода.",
        "theory": (
            "<code>title=\"...\"</code> — подсказка при наведении мыши. Добавляй к ссылкам и кнопкам с неочевидным смыслом.\n\n"
            "<code>aria-label=\"...\"</code> — текстовая метка для скринридеров, когда видимого текста нет. "
            "Пример: кнопка-крестик «✕» без текста — добавь <code>aria-label=\"Закрыть\"</code>.\n\n"
            "<code>aria-hidden=\"true\"</code> — спрятать декоративный элемент от скринридеров (иконки, пустые div).\n\n"
            "<code>tabindex=\"0\"</code> — включить элемент в порядок клавиши Tab (для нестандартных кликабельных элементов). "
            "<code>tabindex=\"-1\"</code> — исключить из Tab-навигации.\n\n"
            "<code>alt</code> у img — тоже aria: скринридер читает alt вместо src."
        ),
        "nuance": (
            "Не перегружай aria: кнопка <code>&lt;button&gt;Закрыть&lt;/button&gt;</code> уже доступна сама по себе. "
            "aria нужна там, где семантики HTML не хватает."
        ),
        "task": (
            "Кнопка-иконка «✕» без видимого текста — добавь <code>aria-label=\"Закрыть\"</code>. "
            "Ссылка на главную — добавь <code>title=\"Перейти на главную\"</code>. "
            "Декоративная картинка (смайлик) — добавь <code>aria-hidden=\"true\"</code> и <code>alt=\"\"</code>."
        ),
        "checklist": [
            "Кнопка с aria-label",
            "Ссылка с title",
            "Элемент с aria-hidden=\"true\"",
        ],
        "hint": "Кнопка: &lt;button aria-label=\"Закрыть\"&gt;✕&lt;/button&gt; — минимальный рабочий пример.",
        "quiz_done_line": (
            "Три атрибута доступности в голове — запиши в файл и посмотри в браузере (через DevTools accessibility). "
            "Потом зачёт модуля HTML!"
        ),
        "win_message": (
            "♿ Ты только что сделал сайт доступным — это то, что отличает профи от новичка.\n"
            "<b>Ты прошёл весь HTML-модуль!</b> 12 уроков — кнопки, ссылки, формы, семантика, доступность.\n"
            "🎨 Следующий модуль — CSS: научишь браузер рисовать красоту поверх этой разметки."
        ),
                "quiz": [
            {
                "q": "Почему <button><img src='search.svg'></button> недостаточно для доступности?",
                "options": [
                    'img нельзя вкладывать в button',
                    'SVG не читается скринридерами',
                    "Скринридер объявит 'кнопка' без описания действия — нужен aria-label",
                    'button должен содержать только текст',
                ],
                "correct": 2,
                "wrong_hint": "Иконка без текста — кнопка без имени. Добавь aria-label='Поиск' или title.",
            },
            {
                "q": "В чём проблема <div onclick='submit()'>Отправить</div> вместо <button>?",
                "options": [
                    'div работает только в Chrome',
                    'button нативно фокусируется Tab, активируется Enter/Space, правильно объявляется скринридером',
                    'onclick не работает на div',
                    'div нельзя стилизовать как кнопку',
                ],
                "correct": 1,
                "wrong_hint": 'Интерактивные элементы должны быть семантически правильными — доступность клавиатуры и ридеров из коробки.',
            },
        ],
    },

    # ════════════════════════════════════════════════════════════════════════
    # MODULE 2 · CSS · 18 уроков
    # ════════════════════════════════════════════════════════════════════════

    "M2-L1": {
        "title": "Подключение CSS и первый селектор",
        "progress_badge": "CSS · 1/18",
        "opening": (
            "❓ Как браузер узнаёт, какой стиль применить к какому тегу?\n"
            "👉 По селектору — правилу, которое говорит «найди этот элемент и покрась его вот так»."
        ),
        "hook": "~4 мин: подключишь CSS к HTML и изменишь цвет текста — первый шаг к живой странице.",
        "theory": (
            "CSS подключается в <code>&lt;head&gt;</code>:\n"
            "<code>&lt;link rel=\"stylesheet\" href=\"style.css\"&gt;</code>\n\n"
            "Структура CSS-правила:\n"
            "<code>селектор { свойство: значение; }</code>\n\n"
            "Типы селекторов:\n"
            "  • <b>По тегу:</b> <code>p { color: red; }</code> — все абзацы.\n"
            "  • <b>По классу:</b> <code>.card { border: 1px solid; }</code> — все с class=\"card\".\n"
            "  • <b>По id:</b> <code>#header { background: blue; }</code> — один элемент.\n"
            "  • <b>Комбинация:</b> <code>div.card { ... }</code> — div с классом card.\n\n"
            "CSS-файл — просто текст с правилами. Комментарий: <code>/* это комментарий */</code>."
        ),
        "nuance": (
            "Можно писать CSS прямо в HTML через атрибут <code>style=\"...\"</code> или тег <code>&lt;style&gt;</code>. "
            "Но внешний файл — правильный способ: один CSS для всего сайта."
        ),
        "task": (
            "Создай style.css, подключи к HTML. Напиши 3 правила: "
            "цвет всех p (color), фон body (background-color), жирность всех h1 (font-weight: bold). "
            "Открой в браузере — изменения должны быть видны."
        ),
        "checklist": [
            "Файл style.css подключён через link в head",
            "Правило для тега p с color",
            "Правило для body с background-color",
            "Правило для h1",
        ],
        "hint": "p { color: steelblue; } — простой рабочий пример.",
        "quiz_done_line": (
            "Структура CSS-правила понятна — создай файл, подключи, проверь. Потом зачёт."
        ),
        "win_message": (
            "🎨 Первые стили написаны — страница ожила!\n"
            "<b>Ты уже умеешь:</b> подключить CSS и применить стиль к тегу, классу, id.\n"
            "Дальше — цвет и фон: научишься управлять визуальной атмосферой страницы."
        ),
                "quiz": [
            {
                "q": 'Порядок специфичности от наименьшей к наибольшей?',
                "options": [
                    'id → class → tag',
                    'tag → id → class',
                    'tag → class → id',
                    'class → tag → id',
                ],
                "correct": 2,
                "wrong_hint": 'Тег (0,0,1) < класс (0,1,0) < id (1,0,0). Inline-style ещё выше.',
            },
            {
                "q": 'Что выберет селектор div > p?',
                "options": [
                    'Все <p> внутри <div> на любой глубине',
                    'Только прямые дочерние <p> элемента <div>',
                    '<div> которые являются детьми <p>',
                    'Первый <p> в каждом <div>',
                ],
                "correct": 1,
                "wrong_hint": '> — комбинатор прямого потомка. Выбирает только непосредственных детей, не внуков.',
            },
        ],
    },

    "M2-L2": {
        "title": "Цвет и фон",
        "progress_badge": "CSS · 2/18",
        "opening": (
            "❓ Сколько способов задать цвет в CSS?\n"
            "👉 Больше пяти. Но используют три основных — и все они нужны в разных ситуациях."
        ),
        "hook": "~4 мин: задашь цвет текста, фона и научишься читать hex-коды — базовый язык дизайна.",
        "theory": (
            "Форматы цвета:\n"
            "  • <b>Именованный:</b> <code>red</code>, <code>steelblue</code>, <code>transparent</code>.\n"
            "  • <b>HEX:</b> <code>#1a2b3c</code> — шестнадцатеричный (RR GG BB). Самый частый.\n"
            "  • <b>RGB:</b> <code>rgb(255, 99, 71)</code> — красный, зелёный, синий от 0 до 255.\n"
            "  • <b>RGBA:</b> <code>rgba(0, 0, 0, 0.5)</code> — то же + прозрачность (0–1).\n"
            "  • <b>HSL:</b> <code>hsl(200, 70%, 50%)</code> — оттенок, насыщенность, яркость.\n\n"
            "Свойства фона:\n"
            "  • <code>background-color</code> — цвет фона.\n"
            "  • <code>background-image: url(...)</code> — изображение как фон.\n"
            "  • <code>background: linear-gradient(to right, #ff6b6b, #4ecdc4)</code> — градиент.\n"
            "  • <code>opacity: 0.5</code> — прозрачность всего элемента (0 = невидим, 1 = непрозрачен)."
        ),
        "nuance": (
            "rgba прозрачность применяется только к цвету. "
            "opacity применяется ко всему элементу включая текст — часто это не то что нужно."
        ),
        "task": (
            "Три блока div: у первого — фон hex-цветом и белый текст, у второго — rgba-фон с прозрачностью 0.6, "
            "у третьего — linear-gradient. Добавь padding: 20px для каждого."
        ),
        "checklist": [
            "Div с hex background-color и контрастным color",
            "Div с rgba background",
            "Div с linear-gradient",
            "У всех div есть padding",
        ],
        "hint": "background: linear-gradient(to right, #667eea, #764ba2); — красивый фиолетовый.",
        "quiz_done_line": (
            "Три формата цвета в голове — вбей в style.css. Потом зачёт."
        ),
        "win_message": (
            "🌈 Цвет — первый инструмент эмоции в дизайне.\n"
            "<b>Ты уже умеешь:</b> задавать цвет любым способом и делать градиенты.\n"
            "Дальше — шрифты: текст станет не просто текстом."
        ),
                "quiz": [
            {
                "q": 'color: rgba(0, 128, 0, 0) — что увидит пользователь?',
                "options": [
                    'Зелёный текст',
                    'Прозрачный текст (полностью невидимый)',
                    'Чёрный текст — rgba не применяется к color',
                    'Ошибку — rgba не поддерживается для color',
                ],
                "correct": 1,
                "wrong_hint": 'Четвёртый параметр alpha=0 означает 100% прозрачность. Текст невидим.',
            },
            {
                "q": 'background: linear-gradient(to right, red, blue) — как выглядит?',
                "options": [
                    'Однотонный красный',
                    'Однотонный синий',
                    'Градиент: красный слева → синий справа',
                    'Градиент: синий слева → красный справа',
                ],
                "correct": 2,
                "wrong_hint": 'to right — направление. Первый цвет (red) слева, последний (blue) справа.',
            },
        ],
    },

    "M2-L3": {
        "title": "Шрифты: family, size, weight, line-height",
        "progress_badge": "CSS · 3/18",
        "opening": (
            "❓ Почему один и тот же текст выглядит по-разному на двух компьютерах?\n"
            "👉 Шрифт не установлен — браузер показывает запасной. Надо подключать явно."
        ),
        "hook": "~4 мин: подключишь Google Fonts и настроишь типографику — страница станет читабельной.",
        "theory": (
            "Основные свойства:\n"
            "  • <code>font-family: 'Inter', sans-serif;</code> — шрифт + запасной.\n"
            "  • <code>font-size: 16px;</code> — размер. rem предпочтительнее px для масштабируемости.\n"
            "  • <code>font-weight: 400;</code> — толщина (100–900). 400 = нормальный, 700 = жирный.\n"
            "  • <code>font-style: italic;</code> — курсив.\n"
            "  • <code>line-height: 1.5;</code> — межстрочный интервал. Без единицы — множитель к font-size.\n"
            "  • <code>letter-spacing: 0.05em;</code> — расстояние между буквами.\n"
            "  • <code>text-align: center;</code> — выравнивание текста (left/center/right/justify).\n"
            "  • <code>text-decoration: none;</code> — убирает подчёркивание у ссылок.\n\n"
            "Подключение Google Fonts (в head HTML):\n"
            "<code>&lt;link href=\"https://fonts.googleapis.com/css2?family=Inter:wght@400;700&amp;display=swap\" rel=\"stylesheet\"&gt;</code>"
        ),
        "nuance": (
            "line-height: 1.5 без единиц — относительно font-size элемента. "
            "line-height: 24px — абсолютный. Относительный — предпочтительнее."
        ),
        "task": (
            "Подключи один шрифт с Google Fonts (например Inter или Roboto). "
            "Примени к body: font-family, font-size: 16px, line-height: 1.6. "
            "К h1: font-size покрупнее, font-weight: 700. "
            "Убери подчёркивание у всех ссылок через text-decoration: none."
        ),
        "checklist": [
            "Google Fonts подключён в head",
            "body с font-family, font-size, line-height",
            "h1 с крупным font-size и font-weight",
            "a с text-decoration: none",
        ],
        "hint": "Inter — универсальный шрифт, смотрится как на крупных продуктах.",
        "quiz_done_line": (
            "Типографика — не украшательство, а читабельность. Вбей и проверь глазами. Потом зачёт."
        ),
        "win_message": (
            "✒️ Подключённый шрифт + правильный line-height — и текст реально хочется читать.\n"
            "<b>Ты уже умеешь:</b> управлять типографикой как в настоящем продукте.\n"
            "Дальше — блочная модель: разберёшься с отступами раз и навсегда."
        ),
                "quiz": [
            {
                "q": 'font-size: 2em у дочернего элемента, у родителя тоже font-size: 2em (32px). Каков размер шрифта ребёнка?',
                "options": [
                    '32px (2em от корневого 16px)',
                    '64px (2em от родительского 32px)',
                    '16px — em всегда от корневого',
                    '2px',
                ],
                "correct": 1,
                "wrong_hint": 'em считается от font-size родителя. 2 × 32px = 64px. Вот почему em накапливается при вложенности.',
            },
            {
                "q": 'Зачем указывать несколько шрифтов через запятую в font-family?',
                "options": [
                    'Браузер выберет самый красивый',
                    'Это устаревший синтаксис',
                    'Если первый шрифт не найден — браузер использует следующий (fallback)',
                    'Ускоряет загрузку',
                ],
                "correct": 2,
                "wrong_hint": 'font-family — список приоритетов. Последний должен быть generic: serif, sans-serif, monospace.',
            },
        ],
    },

    "M2-L4": {
        "title": "Блочная модель: margin, padding, border",
        "progress_badge": "CSS · 4/18",
        "opening": (
            "❓ Почему элемент не там, где ожидаешь?\n"
            "👉 Скорее всего — margin или padding. Понять блочную модель = решить 80% вопросов вёрстки."
        ),
        "hook": "~5 мин: разберёшь блочную модель и научишься предсказывать, где окажется любой элемент.",
        "theory": (
            "Каждый блочный элемент — это коробка с четырьмя зонами (снаружи внутрь):\n\n"
            "  1. <b>margin</b> — внешний отступ (прозрачный, не красится).\n"
            "  2. <b>border</b> — рамка. <code>border: 2px solid #333;</code>\n"
            "  3. <b>padding</b> — внутренний отступ (красится фоном элемента).\n"
            "  4. <b>content</b> — сам контент (текст, изображение).\n\n"
            "Сокращённая запись: <code>margin: 10px 20px;</code> — сверху/снизу 10px, слева/справа 20px.\n"
            "<code>margin: 10px 20px 30px 40px;</code> — верх, право, низ, лево (по часовой).\n\n"
            "<b>box-sizing: border-box</b> — ключевое свойство:\n"
            "По умолчанию (content-box) padding и border увеличивают размер элемента.\n"
            "С border-box — width включает padding и border. Это то, как большинство ожидает.\n"
            "Обычно в начало CSS ставят: <code>*, *::before, *::after { box-sizing: border-box; }</code>"
        ),
        "nuance": (
            "Схлопывание margin: вертикальные margin соседних блоков сливаются в один (берётся бо́льший). "
            "Горизонтальные — нет. Это сбивает с толку — просто знай об этом."
        ),
        "task": (
            "Три div с class=\"box\": ширина 200px, высота 100px, border: 2px solid, padding: 16px, margin: 20px. "
            "Добавь box-sizing: border-box для всех через *. Убедись, что все три вмещаются на экране."
        ),
        "checklist": [
            "* { box-sizing: border-box }",
            "Три div с class box",
            "У div width, height, border, padding, margin",
        ],
        "hint": "Открой DevTools (F12) → Elements → Computed — там видна блочная модель прямо в браузере.",
        "quiz_done_line": (
            "Четыре зоны блока в голове — вбей и проверь через DevTools. Потом зачёт."
        ),
        "win_message": (
            "📦 Блочная модель — самая важная концепция CSS. Ты её знаешь.\n"
            "<b>Ты уже умеешь:</b> управлять отступами предсказуемо, не наугад.\n"
            "Дальше — display: разные режимы отображения элементов."
        ),
                "quiz": [
            {
                "q": 'width: 300px, padding: 20px, border: 5px, box-sizing: content-box. Реальная ширина элемента?',
                "options": [
                    '300px',
                    '320px (300 + 20)',
                    '350px (300 + 20×2 + 5×2)',
                    '295px',
                ],
                "correct": 2,
                "wrong_hint": 'content-box: width = контент. Итог = 300 + 20+20 + 5+5 = 350px. Вот зачем border-box.',
            },
            {
                "q": 'Верхний блок margin-bottom: 30px, нижний margin-top: 20px. Каков реальный отступ между ними?',
                "options": [
                    '50px (суммируются)',
                    '30px (схлопывание — берётся больший)',
                    '20px (берётся меньший)',
                    '0px — margin не работает между блоками',
                ],
                "correct": 1,
                "wrong_hint": 'Вертикальный margin collapsing: смежные margin схлопываются в наибольший из двух.',
            },
        ],
    },

    "M2-L5": {
        "title": "display: block, inline, inline-block, none",
        "progress_badge": "CSS · 5/18",
        "opening": (
            "❓ Почему span не реагирует на width?\n"
            "👉 Потому что он строчный (inline). display управляет «режимом» элемента."
        ),
        "hook": "~4 мин: поймёшь разницу между режимами display — и перестанешь удивляться поведению элементов.",
        "theory": (
            "<code>display: block</code> — занимает всю ширину родителя, начинается с новой строки. "
            "Принимает width, height, margin, padding полностью. Примеры: div, p, h1, section.\n\n"
            "<code>display: inline</code> — ширина по контенту, не начинает новую строку. "
            "Width и height не работают. Вертикальный margin/padding — ограниченно. Примеры: span, a, strong.\n\n"
            "<code>display: inline-block</code> — строчный по поведению (не разрывает поток), "
            "но принимает width, height и все отступы как блочный. Удобно для кнопок в строке.\n\n"
            "<code>display: none</code> — элемент исчезает совсем, место освобождается. "
            "В отличие от <code>visibility: hidden</code> — тот скрывает, но место занимает."
        ),
        "nuance": (
            "inline-block был популярен до Flexbox. Сейчас для layouts используют flex. "
            "Но inline-block всё ещё удобен для аватарок, тегов, значков в строке."
        ),
        "task": (
            "Три span в одной строке — через display: inline-block задай каждому width: 100px, height: 50px, "
            "разные background-color. Рядом — ссылка с display: block, чтобы она заняла всю строку."
        ),
        "checklist": [
            "Три span с display: inline-block, width и height",
            "У span разные цвета фона",
            "Ссылка с display: block",
        ],
        "hint": "span по умолчанию inline — width не работает. Добавь display: inline-block.",
        "quiz_done_line": (
            "Режимы display в голове — вбей в файл и посмотри разницу в браузере. Потом зачёт."
        ),
        "win_message": (
            "🔲 display — один из главных рычагов CSS. Ты его держишь.\n"
            "<b>Ты уже умеешь:</b> менять режим элемента под свою задачу.\n"
            "Дальше — позиционирование: элементы начнут летать."
        ),
                "quiz": [
            {
                "q": 'Почему width: 150px не работает на <span>?',
                "options": [
                    'span не поддерживает свойство width',
                    'span — inline по умолчанию, inline-элементы игнорируют width и height',
                    'width задаётся только через JavaScript для span',
                    'Нужно добавить !important',
                ],
                "correct": 1,
                "wrong_hint": 'Inline-элементы живут в потоке текста — ширину берут из контента. Добавь display: inline-block или block.',
            },
            {
                "q": 'display: none vs visibility: hidden — у обоих элемент невидим. В чём разница?',
                "options": [
                    'Нет разницы',
                    'none убирает элемент из потока (место освобождается); hidden — скрывает, но место сохраняется',
                    'hidden убирает место, none сохраняет',
                    'none работает только в Chrome',
                ],
                "correct": 1,
                "wrong_hint": 'display: none — элемента нет визуально и в потоке. visibility: hidden — невидим, но место занимает.',
            },
        ],
    },

    "M2-L6": {
        "title": "position: relative, absolute, fixed, sticky",
        "progress_badge": "CSS · 6/18",
        "opening": (
            "❓ Как сделать иконку поверх картинки или прилипающую шапку?\n"
            "👉 position. Это самый мощный и самый часто неправильно используемый инструмент CSS."
        ),
        "hook": "~5 мин: разберёшь четыре режима позиционирования и сделаешь элемент поверх другого.",
        "theory": (
            "<code>position: static</code> — по умолчанию, обычный поток документа.\n\n"
            "<code>position: relative</code> — смещается относительно своего обычного места через top/left/right/bottom. "
            "Место в потоке сохраняется. Создаёт контекст для absolute внутри.\n\n"
            "<code>position: absolute</code> — вырывается из потока, позиционируется относительно "
            "ближайшего предка с position != static (или body). "
            "Классика: значок-уведомление поверх аватарки.\n\n"
            "<code>position: fixed</code> — относительно окна браузера, не прокручивается. "
            "Для липких шапок и кнопок «наверх».\n\n"
            "<code>position: sticky</code> — ведёт себя как relative пока не достигнет порога прокрутки, "
            "потом фиксируется. Требует top/left/right/bottom.\n\n"
            "Свойство <code>z-index</code> — порядок по оси Z (кто поверх кого). Работает только с position != static."
        ),
        "nuance": (
            "absolute без position: relative на родителе привязывается к body — "
            "самая частая ошибка. Всегда ставь position: relative на родителе."
        ),
        "task": (
            "Создай «карточку с бейджем»: div.card с position: relative, внутри — картинка или текст, "
            "и span.badge с position: absolute, top: -8px, right: -8px — маленький кружок (width: 20px, "
            "height: 20px, border-radius: 50%, background: red). "
            "Также добавь header с position: sticky, top: 0 и каким-нибудь фоном."
        ),
        "checklist": [
            "div с position: relative",
            "Вложенный элемент с position: absolute",
            "absolute-элемент виден поверх родителя",
            "Элемент с position: sticky и top: 0",
        ],
        "hint": "Добавь большую высоту странице (height: 200vh на body) чтобы проверить sticky.",
        "quiz_done_line": (
            "Четыре режима позиционирования в голове — собери карточку с бейджем. Потом зачёт."
        ),
        "win_message": (
            "📌 Позиционирование — ключ к сложным интерфейсам.\n"
            "<b>Ты уже умеешь:</b> накладывать элементы и фиксировать шапку.\n"
            "Дальше — Flexbox: самый современный способ раскладки."
        ),
                "quiz": [
            {
                "q": 'position: absolute без position на родителях. Относительно чего позиционируется?',
                "options": [
                    'Ближайшего родителя',
                    'Ближайшего предка с position != static (или <body>)',
                    'Viewport (как fixed)',
                    'Первого блочного родителя',
                ],
                "correct": 1,
                "wrong_hint": 'absolute ищет вверх первый positioned предок. Если не найдёт — привяжется к body.',
            },
            {
                "q": 'z-index: 999 не работает. Что скорее всего забыли?',
                "options": [
                    'z-index должен быть ещё больше',
                    'Добавить !important',
                    'z-index работает только при position != static',
                    'z-index устарел',
                ],
                "correct": 2,
                "wrong_hint": 'z-index игнорируется при position: static. Добавь position: relative/absolute/fixed/sticky.',
            },
        ],
    },

    "M2-L7": {
        "title": "Flexbox: контейнер и оси",
        "progress_badge": "CSS · 7/18",
        "opening": (
            "❓ Как выровнять три кнопки по центру одной строкой CSS?\n"
            "👉 display: flex + justify-content: center. Flexbox решает это за секунду."
        ),
        "hook": "~5 мин: поймёшь логику главной и поперечной оси — и большинство раскладок станет очевидными.",
        "theory": (
            "Flexbox — одномерная система раскладки (строка <b>или</b> колонка).\n\n"
            "<code>display: flex</code> на родителе — все прямые дети становятся flex-элементами.\n\n"
            "<b>Свойства контейнера:</b>\n"
            "  • <code>flex-direction</code>: row (→ по умолчанию) | row-reverse | column (↓) | column-reverse.\n"
            "  • <code>justify-content</code>: выравнивание по <b>главной</b> оси:\n"
            "    flex-start | flex-end | center | space-between | space-around | space-evenly.\n"
            "  • <code>align-items</code>: выравнивание по <b>поперечной</b> оси:\n"
            "    stretch (по умолчанию) | flex-start | flex-end | center | baseline.\n"
            "  • <code>gap: 16px</code> — отступ между элементами (современный способ).\n"
            "  • <code>flex-wrap: wrap</code> — перенос на следующую строку, если не влезает.\n\n"
            "Главная ось: направление flex-direction. Поперечная — перпендикулярна главной."
        ),
        "nuance": (
            "justify-content выравнивает по главной оси, align-items — по поперечной. "
            "Перепутать их — самая частая ошибка при изучении Flexbox."
        ),
        "task": (
            "Nav-меню: ul с display: flex, justify-content: space-between, align-items: center, gap: 16px. "
            "Внутри — 4 li с ссылками. Убери маркеры у ul (list-style: none). "
            "Три div-карточки в ряд: контейнер с display: flex, gap: 20px, justify-content: center."
        ),
        "checklist": [
            "ul с display: flex и justify-content",
            "ul с list-style: none",
            "Три div в flex-контейнере с gap",
        ],
        "hint": "align-items: center на ul вертикально выровняет все ссылки.",
        "quiz_done_line": (
            "Главная и поперечная ось в голове — собери меню и три карточки. Потом зачёт."
        ),
        "win_message": (
            "⚡ Flexbox — это то, что ты будешь использовать каждый день.\n"
            "<b>Ты уже умеешь:</b> выстраивать элементы в ряд и управлять выравниванием.\n"
            "Дальше — flex-свойства детей: гибкость каждого элемента."
        ),
                "quiz": [
            {
                "q": 'flex-контейнер с justify-content: space-between и 3 элементами. Как они расположатся?',
                "options": [
                    'Все три по центру',
                    'Все три слева',
                    'Первый слева, последний справа, средний точно посередине',
                    'Равные отступы вокруг каждого элемента (включая края)',
                ],
                "correct": 2,
                "wrong_hint": 'space-between: крайние к стенкам, остальные равномерно между ними. Без отступов у краёв.',
            },
            {
                "q": 'flex-direction: row (по умолчанию). Что выравнивает align-items: center?',
                "options": [
                    'По горизонтали',
                    'По вертикали (поперечная ось)',
                    'И по горизонтали, и по вертикали',
                    'Только текст внутри элементов',
                ],
                "correct": 1,
                "wrong_hint": 'align-items — поперечная ось. При row главная ось горизонтальная, поперечная — вертикальная.',
            },
        ],
    },

    "M2-L8": {
        "title": "Flexbox: flex-grow, flex-shrink, flex-basis",
        "progress_badge": "CSS · 8/18",
        "opening": (
            "❓ Как сделать одну колонку шире другой, не задавая точный процент?\n"
            "👉 flex-grow. Он распределяет свободное место между элементами пропорционально."
        ),
        "hook": "~4 мин: разберёшь три flex-свойства детей — и сможешь строить любые пропорциональные раскладки.",
        "theory": (
            "Свойства flex-<b>элементов</b> (детей контейнера):\n\n"
            "  • <code>flex-grow: 1</code> — элемент растёт на свою долю свободного места. "
            "Если один элемент имеет flex-grow: 2, другой flex-grow: 1 — первый получит вдвое больше.\n"
            "  • <code>flex-shrink: 1</code> — элемент сжимается при нехватке места (по умолчанию 1). "
            "flex-shrink: 0 — не сжимается никогда.\n"
            "  • <code>flex-basis: 200px</code> — начальный размер до распределения. "
            "auto — берётся из width/height.\n\n"
            "Сокращение: <code>flex: 1</code> = flex-grow: 1, flex-shrink: 1, flex-basis: 0.\n\n"
            "  • <code>align-self</code> — переопределяет align-items только для этого элемента.\n"
            "  • <code>order: 2</code> — порядок отображения (не меняет DOM). По умолчанию 0."
        ),
        "nuance": (
            "flex: 1 vs flex-grow: 1: <code>flex: 1</code> ставит flex-basis: 0, "
            "значит пространство делится только по flex-grow. "
            "<code>flex-grow: 1</code> — оставляет flex-basis: auto (учитывает контент)."
        ),
        "task": (
            "Раскладка сайта: header (100%), main (flex-контейнер) с sidebar (width: 200px, flex-shrink: 0) "
            "и content (flex: 1). Sidebar не сжимается, content занимает всё оставшееся место."
        ),
        "checklist": [
            "main с display: flex",
            "sidebar с фиксированной шириной и flex-shrink: 0",
            "content с flex: 1",
        ],
        "hint": "sidebar { width: 200px; flex-shrink: 0; } content { flex: 1; }",
        "quiz_done_line": (
            "flex-grow и flex-shrink в голове — собери sidebar + content. Потом зачёт."
        ),
        "win_message": (
            "🏗 Sidebar + content за 3 строки CSS — это сила Flexbox.\n"
            "<b>Ты уже умеешь:</b> строить классическую двухколоночную раскладку.\n"
            "Дальше — CSS Grid: двумерная сетка для более сложных layout."
        ),
                "quiz": [
            {
                "q": 'Два flex-элемента: flex-grow: 2 и flex-grow: 1. Свободного места 90px. Сколько получит первый?',
                "options": [
                    '45px',
                    '60px',
                    '90px',
                    '30px',
                ],
                "correct": 1,
                "wrong_hint": 'Пропорция 2:1. Всего 3 части. 90px ÷ 3 × 2 = 60px первому, 30px второму.',
            },
            {
                "q": 'flex: 1 — что это означает?',
                "options": [
                    'flex-grow: 1, flex-shrink: 0, flex-basis: auto',
                    'flex-grow: 1, flex-shrink: 1, flex-basis: 0',
                    'width: 1fr',
                    'flex-grow: 0, flex-shrink: 1, flex-basis: 100%',
                ],
                "correct": 1,
                "wrong_hint": 'flex: 1 = grow:1, shrink:1, basis:0. Элемент растёт и сжимается, занимая доступное место.',
            },
        ],
    },

    "M2-L9": {
        "title": "CSS Grid: строки, колонки, области",
        "progress_badge": "CSS · 9/18",
        "opening": (
            "❓ Чем Grid отличается от Flexbox?\n"
            "👉 Flexbox — одно направление. Grid — две оси одновременно: строки И колонки."
        ),
        "hook": "~5 мин: создашь сетку из колонок и разместишь элементы по ячейкам — как в дизайне.",
        "theory": (
            "<code>display: grid</code> на родителе.\n\n"
            "<b>Определение сетки:</b>\n"
            "  • <code>grid-template-columns: 1fr 1fr 1fr</code> — три равные колонки.\n"
            "  • <code>grid-template-columns: repeat(3, 1fr)</code> — то же короче.\n"
            "  • <code>grid-template-columns: 200px 1fr</code> — 200px + остаток.\n"
            "  • <code>grid-template-rows: auto</code> — высота строк по контенту.\n"
            "  • <code>gap: 20px</code> — отступы между ячейками.\n\n"
            "<b>Размещение элементов:</b>\n"
            "  • <code>grid-column: 1 / 3</code> — элемент занимает колонки с 1 по 3 (т.е. 2 колонки).\n"
            "  • <code>grid-row: 1 / 2</code> — строка.\n"
            "  • <code>grid-column: span 2</code> — занять 2 колонки подряд.\n\n"
            "<b>Именованные области:</b>\n"
            "<code>grid-template-areas: \"header header\" \"sidebar main\" \"footer footer\"</code>\n"
            "Элемент: <code>grid-area: header;</code>"
        ),
        "nuance": (
            "fr (fractional unit) — доля свободного пространства. "
            "1fr 2fr 1fr — три колонки, средняя вдвое шире крайних."
        ),
        "task": (
            "Сетка 3 колонки: grid-template-columns: repeat(3, 1fr), gap: 16px. "
            "Шесть div-карточек (заполнят сетку). "
            "Первую карточку сделай grid-column: span 2 (занимает 2 колонки)."
        ),
        "checklist": [
            "Контейнер с display: grid",
            "grid-template-columns с тремя колонками",
            "gap между карточками",
            "Первый элемент с grid-column: span 2",
        ],
        "hint": "repeat(3, 1fr) — три равных колонки без магических чисел.",
        "quiz_done_line": (
            "Grid-сетка в голове — собери 6 карточек с span-элементом. Потом зачёт."
        ),
        "win_message": (
            "🔲 CSS Grid — главный инструмент страничных раскладок.\n"
            "<b>Ты уже умеешь:</b> строить сетки и растягивать элементы через несколько ячеек.\n"
            "Дальше — единицы измерения: px, %, rem, vw/vh."
        ),
                "quiz": [
            {
                "q": 'grid-template-columns: repeat(4, 1fr) — сколько колонок и какие?',
                "options": [
                    '1 колонка шириной 4fr',
                    '4 одинаковые колонки, делящие пространство поровну',
                    '4 колонки по 1px',
                    'Колонки по размеру контента',
                ],
                "correct": 1,
                "wrong_hint": 'repeat(4, 1fr) = 4 колонки по одной доле от доступного пространства.',
            },
            {
                "q": 'grid-column: 1 / -1 у элемента сетки — что означает?',
                "options": [
                    'Занимает первую и последнюю колонку отдельно',
                    'Ошибка — -1 не поддерживается',
                    'Растягивается на все колонки (от линии 1 до последней линии)',
                    'Элемент скрыт',
                ],
                "correct": 2,
                "wrong_hint": '-1 — последняя линия сетки. Элемент занимает весь ряд целиком.',
            },
        ],
    },

    "M2-L10": {
        "title": "Единицы: px, %, rem, em, vw/vh",
        "progress_badge": "CSS · 10/18",
        "opening": (
            "❓ Почему один и тот же padding выглядит по-разному на разных шрифтах?\n"
            "👉 Потому что px — абсолютное, а em — относительное к font-size. Это разные инструменты."
        ),
        "hook": "~4 мин: разберёшь 5 единиц измерения и поймёшь, когда какую брать.",
        "theory": (
            "<b>Абсолютные:</b>\n"
            "  • <code>px</code> — пиксель. Точно, но не масштабируется с настройками браузера.\n\n"
            "<b>Относительные:</b>\n"
            "  • <code>%</code> — процент от родителя (width: 50% — половина родителя).\n"
            "  • <code>em</code> — относительно <code>font-size текущего элемента</code>. "
            "padding: 1em = один размер шрифта этого элемента. Опасен при вложении.\n"
            "  • <code>rem</code> — относительно <code>font-size корневого &lt;html&gt;</code>. "
            "По умолчанию 1rem = 16px. Предпочтительнее em для типографики.\n\n"
            "<b>Вьюпортные:</b>\n"
            "  • <code>vw</code> — 1% ширины окна браузера.\n"
            "  • <code>vh</code> — 1% высоты окна. height: 100vh — на весь экран.\n\n"
            "<b>Когда что:</b>\n"
            "  • px — границы, тени, маленькие элементы.\n"
            "  • rem — font-size, крупные отступы.\n"
            "  • % — ширины колонок, адаптивные блоки.\n"
            "  • vw/vh — hero-секции, полноэкранные блоки."
        ),
        "nuance": (
            "font-size: 62.5% на html переводит rem в «удобные» числа: 1rem = 10px, 1.6rem = 16px. "
            "Это популярный трюк для читабельного кода."
        ),
        "task": (
            "Элемент с height: 100vh (на весь экран). "
            "Внутри — текст с font-size: 2rem. "
            "Отступы padding в rem (не px). "
            "Дочерний блок с width: 50% (половина родителя)."
        ),
        "checklist": [
            "Элемент с height: 100vh",
            "Текст с font-size в rem",
            "padding в rem",
            "Дочерний элемент с width в процентах",
        ],
        "hint": "html { font-size: 16px; } — базовый размер, от него считается rem.",
        "quiz_done_line": (
            "Пять единиц в голове — вбей в файл, поиграй с браузером. Потом зачёт."
        ),
        "win_message": (
            "📐 Единицы измерения — словарь точного CSS.\n"
            "<b>Ты уже умеешь:</b> выбирать единицу под задачу, а не брать px всегда.\n"
            "Дальше — псевдоклассы и псевдоэлементы: интерактивность без JS."
        ),
                "quiz": [
            {
                "q": '1rem по умолчанию равен?',
                "options": [
                    '10px',
                    '12px',
                    '16px',
                    '14px',
                ],
                "correct": 2,
                "wrong_hint": 'По умолчанию font-size корневого html = 16px. 1rem = 16px если не переопределять.',
            },
            {
                "q": 'height: 100vh vs height: 100% — в чём разница?',
                "options": [
                    'Нет разницы',
                    '100vh — высота viewport; 100% — от родителя (родитель обязан иметь высоту)',
                    '100% всегда больше 100vh',
                    '100vh работает только в Chrome',
                ],
                "correct": 1,
                "wrong_hint": '100vh = высота экрана всегда. 100% = от родителя — если у родителя нет высоты, не сработает.',
            },
        ],
    },

    "M2-L11": {
        "title": "Псевдоклассы и псевдоэлементы",
        "progress_badge": "CSS · 11/18",
        "opening": (
            "❓ Как изменить цвет ссылки при наведении без JS?\n"
            "👉 Псевдокласс :hover. CSS сам следит за состоянием элемента."
        ),
        "hook": "~4 мин: добавишь интерактивность через псевдоклассы и декоративные элементы без лишнего HTML.",
        "theory": (
            "<b>Псевдоклассы</b> — состояния элемента:\n"
            "  • <code>:hover</code> — курсор над элементом.\n"
            "  • <code>:focus</code> — элемент в фокусе (Tab или клик).\n"
            "  • <code>:active</code> — в момент нажатия.\n"
            "  • <code>:visited</code> — посещённая ссылка.\n"
            "  • <code>:first-child</code>, <code>:last-child</code> — первый/последний потомок.\n"
            "  • <code>:nth-child(2n)</code> — каждый второй (чётные строки таблицы).\n"
            "  • <code>:not(.active)</code> — все элементы кроме .active.\n\n"
            "<b>Псевдоэлементы</b> — несуществующие части элемента:\n"
            "  • <code>::before</code>, <code>::after</code> — вставляют контент до/после (нужен content: \"\").\n"
            "  • <code>::placeholder</code> — стиль текста-подсказки в input.\n"
            "  • <code>::first-line</code>, <code>::first-letter</code> — первая строка/буква."
        ),
        "nuance": (
            "::before и ::after — display: inline по умолчанию. "
            "Часто добавляют display: block или display: flex для управления размером."
        ),
        "task": (
            "Кнопка с :hover (меняет цвет и cursor: pointer). "
            "Input с :focus (border-color меняется). "
            "Список li:nth-child(odd) с полосатым фоном. "
            "Заголовок h2::before с иконкой через content."
        ),
        "checklist": [
            "Кнопка с :hover стилями",
            "Input с :focus стилями",
            "li:nth-child с чередующимся фоном",
            "h2::before или ::after с content",
        ],
        "hint": "button:hover { background: darkblue; cursor: pointer; }",
        "quiz_done_line": (
            "Псевдоклассы и псевдоэлементы понятны — соберёт интерактивный список. Потом зачёт."
        ),
        "win_message": (
            "✨ Интерактивность без JS — это красиво и правильно.\n"
            "<b>Ты уже умеешь:</b> делать hover-эффекты, полосатые таблицы и декоративные элементы.\n"
            "Дальше — transition и animation: движение на странице."
        ),
                "quiz": [
            {
                "q": '::before не отображается на странице. Что скорее всего забыли?',
                "options": [
                    'display: block',
                    'position: absolute',
                    "Свойство content (хотя бы content: '')",
                    'color: inherit',
                ],
                "correct": 2,
                "wrong_hint": 'content обязателен для ::before и ::after. Без него псевдоэлемент не создаётся.',
            },
            {
                "q": ':nth-child(2n+1) выбирает?',
                "options": [
                    'Каждый второй элемент',
                    'Нечётные: 1-й, 3-й, 5-й и т.д.',
                    'Каждый третий',
                    'Только первый',
                ],
                "correct": 1,
                "wrong_hint": '2n+1 при n=0,1,2... даёт 1,3,5... — нечётные элементы. Синоним: :nth-child(odd).',
            },
        ],
    },

    "M2-L12": {
        "title": "Transition и animation",
        "progress_badge": "CSS · 12/18",
        "opening": (
            "❓ Почему анимация делает интерфейс приятнее?\n"
            "👉 Она показывает, что произошло. Без неё изменения — резкие и непонятные."
        ),
        "hook": "~5 мин: добавишь плавный hover и keyframe-анимацию — страница оживёт.",
        "theory": (
            "<b>transition</b> — плавный переход при изменении свойства:\n"
            "<code>transition: all 0.3s ease;</code>\n"
            "Синтаксис: <code>transition: свойство длительность функция задержка</code>\n"
            "Функции: ease, linear, ease-in, ease-out, ease-in-out, cubic-bezier(...).\n\n"
            "Несколько: <code>transition: color 0.2s ease, transform 0.3s ease-out;</code>\n\n"
            "<b>transform</b> — трансформации без сдвига потока:\n"
            "  • <code>scale(1.05)</code> — увеличить.\n"
            "  • <code>translateY(-4px)</code> — сдвинуть вверх.\n"
            "  • <code>rotate(45deg)</code> — повернуть.\n\n"
            "<b>@keyframes</b> — анимация по ключевым кадрам:\n"
            "<code>@keyframes fade-in { from { opacity: 0; } to { opacity: 1; } }</code>\n"
            "<code>animation: fade-in 0.5s ease forwards;</code>\n"
            "Свойства: name duration timing-function delay iteration-count direction fill-mode."
        ),
        "nuance": (
            "Анимируй только transform и opacity — они не вызывают reflow/repaint, "
            "браузер обрабатывает их на GPU. Анимация width или top — дорого."
        ),
        "task": (
            "Карточка с transition: transform 0.3s ease и :hover { transform: translateY(-6px) scale(1.02); }. "
            "Отдельный элемент с @keyframes pulse (пульсирующий масштаб 1 → 1.05 → 1) и "
            "animation: pulse 1.5s ease-in-out infinite."
        ),
        "checklist": [
            "Карточка с transition на transform",
            ":hover изменяет transform",
            "@keyframes с названием",
            "Элемент с animation и infinite",
        ],
        "hint": "Сначала сделай transition + hover, потом добавь @keyframes пульс.",
        "quiz_done_line": (
            "transition и @keyframes в руках — добавь движение. Потом зачёт."
        ),
        "win_message": (
            "🎬 Анимации делают интерфейс живым — и теперь ты умеешь их делать.\n"
            "<b>Ты уже умеешь:</b> плавные переходы и keyframe-анимацию.\n"
            "Дальше — медиазапросы: сайт начнёт работать на телефонах."
        ),
                "quiz": [
            {
                "q": 'Зачем анимировать transform вместо left/top?',
                "options": [
                    'Синтаксис проще',
                    'left/top не работают без position',
                    'transform обрабатывается GPU без reflow — анимация плавнее и быстрее',
                    'Нет разницы в производительности',
                ],
                "correct": 2,
                "wrong_hint": 'Изменение left/top запускает reflow (пересчёт layout). Transform — только compositing на GPU.',
            },
            {
                "q": 'animation-fill-mode: forwards — что означает?',
                "options": [
                    'Анимация воспроизводится в прямом направлении',
                    'После завершения элемент сохраняет стили последнего кадра',
                    'Анимация зациклена бесконечно',
                    'Анимация задержана до forwards',
                ],
                "correct": 1,
                "wrong_hint": 'forwards — элемент «замирает» в последнем кадре анимации вместо возврата к начальному состоянию.',
            },
        ],
    },

    "M2-L13": {
        "title": "Media queries: адаптивная вёрстка",
        "progress_badge": "CSS · 13/18",
        "opening": (
            "❓ Почему сайт хорошо выглядит на десктопе, но ломается на телефоне?\n"
            "👉 Нет медиазапросов — CSS одинаков для всех экранов. Media queries это исправляют."
        ),
        "hook": "~5 мин: напишешь первый breakpoint и сетка сама перестроится для мобильного экрана.",
        "theory": (
            "Media query — условие, при котором применяется блок CSS:\n"
            "<code>@media (max-width: 768px) { ... }</code>\n\n"
            "<b>Стратегии:</b>\n"
            "  • Mobile-first: пишешь базовый CSS для мобайла, потом min-width для десктопа.\n"
            "  • Desktop-first: пишешь для десктопа, потом max-width для мобайла.\n"
            "Mobile-first — предпочтительнее в современных проектах.\n\n"
            "<b>Типовые breakpoints:</b>\n"
            "  • 480px — маленькие телефоны.\n"
            "  • 768px — планшеты.\n"
            "  • 1024px — ноутбуки.\n"
            "  • 1280px — десктоп.\n\n"
            "<b>Что меняют в медиазапросах:</b>\n"
            "  • grid-template-columns: 1fr (одна колонка вместо трёх).\n"
            "  • font-size: меньше.\n"
            "  • Навигация: column вместо row.\n"
            "  • display: none — скрыть/показать блоки."
        ),
        "nuance": (
            "Не забудь meta viewport в head — без него браузер масштабирует сайт "
            "как будто экран 980px, и медиазапросы не сработают нужным образом."
        ),
        "task": (
            "Сетка 3 колонки на десктопе. "
            "@media (max-width: 768px) — 2 колонки. "
            "@media (max-width: 480px) — 1 колонка. "
            "Навигация: flex-direction: row на десктопе, column на мобиле."
        ),
        "checklist": [
            "Базовый grid с тремя колонками",
            "@media max-width: 768px с двумя колонками",
            "@media max-width: 480px с одной колонкой",
            "Навигация меняет направление в медиазапросе",
        ],
        "hint": "Открой DevTools и переключи на мобильный вид (Ctrl+Shift+M) — увидишь breakpoints в действии.",
        "quiz_done_line": (
            "Три breakpoint в голове — проверь в DevTools на разных ширинах. Потом зачёт."
        ),
        "win_message": (
            "📱 Твой сайт теперь работает на телефоне — это важнее, чем выглядеть на десктопе.\n"
            "<b>Ты уже умеешь:</b> строить адаптивные раскладки с breakpoints.\n"
            "Дальше — CSS-переменные: код станет проще и мощнее."
        ),
                "quiz": [
            {
                "q": 'Mobile-first подход в CSS означает?',
                "options": [
                    'Сначала делаешь десктопную версию, потом мобильную',
                    'Базовые стили для мобиле, min-width медиазапросы расширяют для больших экранов',
                    'Тестируешь только на телефоне',
                    'Не используешь фиксированные размеры',
                ],
                "correct": 1,
                "wrong_hint": 'Mobile-first: пишешь базовый CSS для мобиле, добавляешь @media (min-width:...) для планшетов и десктопа.',
            },
            {
                "q": "Что случится на мобиле без <meta name='viewport'>?",
                "options": [
                    'Страница отобразится нормально',
                    'CSS не применится',
                    'Браузер масштабирует страницу как десктопную — текст крошечный',
                    'Изображения не загрузятся',
                ],
                "correct": 2,
                "wrong_hint": 'Мобильный браузер рендерит страницу как ~980px и уменьшает — всё нечитаемо мелкое.',
            },
        ],
    },

    "M2-L14": {
        "title": "CSS-переменные (Custom Properties)",
        "progress_badge": "CSS · 14/18",
        "opening": (
            "❓ Как изменить основной цвет на всём сайте одной строкой?\n"
            "👉 CSS-переменная. Меняешь в одном месте — работает везде."
        ),
        "hook": "~4 мин: создашь дизайн-токены через переменные — профессиональный подход к теме.",
        "theory": (
            "CSS Custom Properties — переменные внутри CSS:\n\n"
            "<b>Объявление</b> (обычно на :root):\n"
            "<code>:root {\n"
            "  --color-primary: #4f46e5;\n"
            "  --color-bg: #f8fafc;\n"
            "  --spacing-md: 16px;\n"
            "  --radius: 8px;\n"
            "  --font-size-base: 1rem;\n"
            "}</code>\n\n"
            "<b>Использование:</b>\n"
            "<code>button { background: var(--color-primary); border-radius: var(--radius); }</code>\n\n"
            "<b>Запасное значение:</b>\n"
            "<code>color: var(--color-text, #333);</code> — если переменная не определена, берётся #333.\n\n"
            "<b>Переопределение в компоненте:</b>\n"
            "<code>.card { --color-primary: green; }</code> — внутри .card переменная другая.\n\n"
            "<b>JS-интеграция:</b>\n"
            "<code>document.documentElement.style.setProperty('--color-primary', '#ff0000');</code>"
        ),
        "nuance": (
            "CSS-переменные каскадируются как обычные свойства. "
            "Переменная, объявленная на :root, доступна везде. "
            "На конкретном элементе — только в нём и его потомках."
        ),
        "task": (
            "В :root объяви 4–5 переменных: основной цвет, цвет фона, радиус скругления, базовый отступ, шрифт. "
            "Используй их в кнопках, карточках, body. "
            "Сделай тёмную тему: класс .dark-theme на body переопределяет цветовые переменные."
        ),
        "checklist": [
            ":root с минимум 4 CSS-переменными",
            "Кнопки и карточки используют var(--...)",
            "Класс .dark-theme переопределяет цвета",
        ],
        "hint": ":root { --bg: #fff; --text: #111; } .dark-theme { --bg: #111; --text: #eee; }",
        "quiz_done_line": (
            "CSS-переменные — мощный инструмент. Собери тёмную тему. Потом зачёт."
        ),
        "win_message": (
            "🎨 Дизайн-токены через переменные — это то, как делают в Figma и реальных дизайн-системах.\n"
            "<b>Ты уже умеешь:</b> строить тематизированный CSS с одной точкой изменения.\n"
            "Дальше — специфичность: почему твои стили иногда не применяются."
        ),
                "quiz": [
            {
                "q": 'Как переопределить CSS-переменную --gap: 16px только для мобиле?',
                "options": [
                    'Нельзя — переменные неизменяемы',
                    '@media (max-width: 768px) { :root { --gap: 8px; } }',
                    'Через JavaScript только',
                    'Добавить !important к значению',
                ],
                "correct": 1,
                "wrong_hint": 'CSS-переменные можно переопределять в медиазапросах — это одно из их главных преимуществ.',
            },
            {
                "q": 'var(--color, blue) — что значит blue?',
                "options": [
                    'Ошибка — var не принимает второй аргумент',
                    'Значение по умолчанию, если --color не определена',
                    'Перезаписывает значение переменной',
                    'Комментарий',
                ],
                "correct": 1,
                "wrong_hint": 'Второй аргумент var() — fallback. Если переменная не найдена, применится значение по умолчанию.',
            },
        ],
    },

    "M2-L15": {
        "title": "Специфичность и каскад",
        "progress_badge": "CSS · 15/18",
        "opening": (
            "❓ Почему ты добавил стиль, а он не применился?\n"
            "👉 Его перебил более специфичный селектор. Специфичность — иерархия правил CSS."
        ),
        "hook": "~4 мин: разберёшь, почему одни стили перебивают другие — и перестанешь добавлять !important.",
        "theory": (
            "<b>Порядок приоритетов (от слабейшего к сильнейшему):</b>\n"
            "  1. Браузерные стили по умолчанию.\n"
            "  2. Внешний CSS-файл.\n"
            "  3. Стили внутри тега &lt;style&gt;.\n"
            "  4. Inline-стили (style=\"...\").\n"
            "  5. !important (крайний случай).\n\n"
            "<b>Специфичность селекторов (баллы):</b>\n"
            "  • Тег (p, div): 0-0-1.\n"
            "  • Класс (.card), псевдокласс (:hover): 0-1-0.\n"
            "  • ID (#header): 1-0-0.\n"
            "  • Inline: выше всех.\n\n"
            "Считается: div.card p = 0-1-2 (1 класс + 2 тега). Побеждает правило с большим числом.\n\n"
            "<b>Каскад:</b> при одинаковой специфичности побеждает правило, написанное позже в CSS."
        ),
        "nuance": (
            "!important нарушает каскад и превращает CSS в хаос. "
            "Вместо !important — повышай специфичность добавлением класса или "
            "реструктурируй CSS так, чтобы нужное правило шло позже."
        ),
        "task": (
            "Напиши 3 правила для одного элемента с разной специфичностью: по тегу, по классу, по ID. "
            "Убедись, что id-стиль побеждает. Попробуй перебить id через inline style. "
            "Объясни в комментарии CSS, почему каждое правило выигрывает."
        ),
        "checklist": [
            "Три правила для одного элемента с разной специфичностью",
            "id-стиль перебивает class-стиль",
            "inline style перебивает id",
            "Комментарии объясняют приоритет",
        ],
        "hint": "Открой DevTools — в Styles видно, какие правила перебиты (зачёркнуты).",
        "quiz_done_line": (
            "Специфичность — логична, не магия. Собери три конфликтующих правила. Потом зачёт."
        ),
        "win_message": (
            "⚖️ Ты понимаешь каскад — теперь CSS предсказуем, не случаен.\n"
            "<b>Ты уже умеешь:</b> читать конфликты стилей и решать их без !important.\n"
            "Дальше — работа с изображениями в CSS: object-fit и background-size."
        ),
                "quiz": [
            {
                "q": 'Стили: .card { color: red } и #header .card { color: blue }. Какой цвет?',
                "options": [
                    'Красный — написан первым',
                    'Синий — id даёт высокую специфичность',
                    'Зависит от порядка в файле',
                    'Наследуется от родителя',
                ],
                "correct": 1,
                "wrong_hint": '#header .card: специфичность (1,1,0). .card: (0,1,0). Побеждает id — цвет синий.',
            },
            {
                "q": 'Когда использовать !important?',
                "options": [
                    'Для всех важных стилей',
                    'Для CSS-переменных',
                    'Только как крайняя мера — перебивает всю специфичность и ломает каскад',
                    'Для стилей которые нельзя менять',
                ],
                "correct": 2,
                "wrong_hint": '!important — ядерное оружие CSS. Если вынужден его использовать — скорее всего проблема в архитектуре.',
            },
        ],
    },

    "M2-L16": {
        "title": "Изображения: object-fit, border-radius, тени",
        "progress_badge": "CSS · 16/18",
        "opening": (
            "❓ Почему аватарка растягивается и выглядит уродливо?\n"
            "👉 Нет object-fit. Одно свойство — и изображение красиво вписывается в контейнер."
        ),
        "hook": "~4 мин: научишься делать круглые аватарки, красивые карточки с изображениями и тени.",
        "theory": (
            "<b>object-fit</b> — как img/video заполняет контейнер:\n"
            "  • <code>cover</code> — заполняет контейнер, обрезает лишнее (как background-size: cover).\n"
            "  • <code>contain</code> — вписывается целиком, могут быть поля.\n"
            "  • <code>fill</code> — растягивается (по умолчанию, часто некрасиво).\n"
            "  • <code>none</code> — оригинальный размер.\n\n"
            "<b>object-position</b>: <code>center top</code> — откуда обрезаем при cover.\n\n"
            "<b>border-radius:</b>\n"
            "  • <code>border-radius: 8px</code> — скруглённые углы.\n"
            "  • <code>border-radius: 50%</code> — круг (если ширина = высота).\n\n"
            "<b>box-shadow:</b>\n"
            "<code>box-shadow: 0 4px 16px rgba(0,0,0,0.12);</code>\n"
            "Синтаксис: offset-x offset-y blur spread color.\n"
            "Несколько теней: через запятую.\n\n"
            "<b>text-shadow:</b> <code>text-shadow: 1px 2px 4px rgba(0,0,0,0.3);</code>"
        ),
        "nuance": (
            "overflow: hidden на контейнере нужен для скругления через border-radius, "
            "если содержимое выходит за пределы."
        ),
        "task": (
            "Карточка: img с object-fit: cover, высота 200px, border-radius: 8px 8px 0 0 (скруглены верхние углы). "
            "box-shadow на карточке. "
            "Аватарка: круглая (border-radius: 50%, width: 80px, height: 80px, object-fit: cover)."
        ),
        "checklist": [
            "img с object-fit: cover и фиксированной высотой",
            "border-radius на карточке",
            "box-shadow на карточке",
            "Круглая аватарка с border-radius: 50%",
        ],
        "hint": "overflow: hidden на контейнере — если border-radius не обрезает картинку.",
        "quiz_done_line": (
            "object-fit и border-radius в руках — собери карточку и аватарку. Потом зачёт."
        ),
        "win_message": (
            "🖼 Теперь изображения выглядят как у больших.\n"
            "<b>Ты уже умеешь:</b> вписывать фото в контейнер красиво и делать круглые аватарки.\n"
            "Дальше — стилизация форм: формы перестанут быть уродливыми."
        ),
                "quiz": [
            {
                "q": 'object-fit: cover на img с заданными width и height — что происходит?',
                "options": [
                    'Картинка сжимается с искажением пропорций',
                    'Картинка кадрируется по центру без искажений, заполняя контейнер',
                    'Появляются белые поля по бокам',
                    'Картинка растягивается до размера контейнера',
                ],
                "correct": 1,
                "wrong_hint": 'cover: изображение масштабируется сохраняя пропорции и кадрируется чтобы полностью заполнить блок.',
            },
            {
                "q": 'Как сделать идеальный круг из div 100×100?',
                "options": [
                    'border-radius: 100px',
                    'border-radius: 50%',
                    'circle: true',
                    'clip-path: circle()',
                ],
                "correct": 1,
                "wrong_hint": 'border-radius: 50% от квадратного элемента всегда даёт идеальный круг независимо от размера.',
            },
        ],
    },

    "M2-L17": {
        "title": "Стилизация форм",
        "progress_badge": "CSS · 17/18",
        "opening": (
            "❓ Почему формы в браузере выглядят так по-разному?\n"
            "👉 У каждого браузера свои стили по умолчанию. CSS сбрасывает их и даёт единый вид."
        ),
        "hook": "~5 мин: превратишь серую браузерную форму в красивый UI-компонент.",
        "theory": (
            "<b>Сброс стилей браузера:</b>\n"
            "<code>input, textarea, select, button {\n"
            "  font: inherit;\n"
            "  border: none;\n"
            "  outline: none;\n"
            "  box-sizing: border-box;\n"
            "}</code>\n\n"
            "<b>Стилизация input:</b>\n"
            "<code>input[type=\"text\"] {\n"
            "  width: 100%;\n"
            "  padding: 10px 14px;\n"
            "  border: 1.5px solid #d1d5db;\n"
            "  border-radius: 6px;\n"
            "  transition: border-color 0.2s;\n"
            "}\n"
            "input[type=\"text\"]:focus {\n"
            "  border-color: #4f46e5;\n"
            "  outline: none;\n"
            "}</code>\n\n"
            "<b>Кнопка submit:</b>\n"
            "<code>button[type=\"submit\"] {\n"
            "  background: var(--color-primary, #4f46e5);\n"
            "  color: #fff;\n"
            "  padding: 10px 24px;\n"
            "  border-radius: 6px;\n"
            "  cursor: pointer;\n"
            "  transition: background 0.2s;\n"
            "}\n"
            "button[type=\"submit\"]:hover { background: #3730a3; }</code>\n\n"
            "Атрибутный селектор <code>input[type=\"text\"]</code> — только нужный тип."
        ),
        "nuance": (
            "outline: none убирает синюю рамку focus — но обязательно добавь свой стиль :focus. "
            "Без видимого фокуса форма становится недоступной для клавиатурной навигации."
        ),
        "task": (
            "Форма обратной связи: поля «Имя» (input text) и «Сообщение» (textarea), кнопка «Отправить». "
            "Все поля: padding, border, border-radius, transition на :focus. "
            "Кнопка: красивый фон, hover-эффект. "
            "Общий контейнер: max-width: 400px, margin: 0 auto."
        ),
        "checklist": [
            "input и textarea с padding и border",
            ":focus меняет border-color",
            "Кнопка с фоном и hover-эффектом",
            "Контейнер с max-width и auto-margin",
        ],
        "hint": "margin: 0 auto — центрирует блочный элемент с фиксированной шириной.",
        "quiz_done_line": (
            "Стилизованная форма — финальный штрих CSS. Собери и посмотри в браузере. Потом зачёт."
        ),
        "win_message": (
            "📋 Красивая форма — узнаваемый признак внимательного разработчика.\n"
            "<b>Ты уже умеешь:</b> полностью стилизовать форму под любой дизайн.\n"
            "Следующий урок — итоговый проект CSS: собираешь настоящий лендинг."
        ),
                "quiz": [
            {
                "q": 'Почему убирают outline на :focus — и что обязательно добавляют взамен?',
                "options": [
                    'outline убирают и не заменяют — он некрасивый',
                    'Убирают стандартный синий ободок, но ОБЯЗАНЫ добавить кастомный стиль фокуса',
                    'outline убирают для ускорения сайта',
                    'Это ошибка — outline нельзя убирать',
                ],
                "correct": 1,
                "wrong_hint": 'Убрать outline без замены = сломать клавиатурную навигацию. Добавляй custom :focus стиль.',
            },
            {
                "q": "input[type='email'] vs просто input — зачем конкретный селектор?",
                "options": [
                    'Нет разницы',
                    'Быстрее вычисляется браузером',
                    'Стиль применится только к email-полям, не затрагивая checkbox, radio и другие',
                    'input[type] устаревший синтаксис',
                ],
                "correct": 2,
                "wrong_hint": 'Атрибутный селектор — точный выбор. input без type стилизует все поля включая checkbox.',
            },
        ],
    },

    "M2-L18": {
        "title": "Итог CSS: собираем лендинг",
        "progress_badge": "CSS · 18/18",
        "opening": (
            "❓ Как проверить, что действительно всё понял?\n"
            "👉 Собрать страницу с нуля. Сейчас ты это сделаешь."
        ),
        "hook": "~15 мин: один настоящий лендинг с нуля — все 17 уроков CSS в одном проекте.",
        "theory": (
            "Структура типичного лендинга:\n\n"
            "<b>1. Hero-секция:</b> 100vh, центрированный текст (flexbox), фоновый градиент.\n"
            "<b>2. Навигация:</b> sticky top:0, flexbox, backdrop-filter: blur(8px).\n"
            "<b>3. Преимущества:</b> grid 3 колонки, карточки с border-radius, box-shadow.\n"
            "<b>4. Форма:</b> стилизованные поля, кнопка с hover.\n"
            "<b>5. Footer:</b> тёмный фон, текст по центру.\n\n"
            "<b>Чеклист качества:</b>\n"
            "  ✓ box-sizing: border-box на всём (*).\n"
            "  ✓ CSS-переменные для цветов и отступов.\n"
            "  ✓ Шрифт с Google Fonts.\n"
            "  ✓ Адаптивность: 2 брейкпоинта.\n"
            "  ✓ Hover-эффекты на кнопках и карточках.\n"
            "  ✓ Изображения с object-fit: cover.\n"
            "  ✓ Плавные transition."
        ),
        "nuance": (
            "Не пытайся сделать всё идеально за один раз. "
            "Структура → стили → детали. Итерируй."
        ),
        "task": (
            "Лендинг: hero (100vh, gradient, h1 + p + button по центру), "
            "секция преимуществ (grid 3 карточки с иконкой/заголовком/текстом), "
            "форма обратной связи (2 поля + кнопка), footer. "
            "Адаптивность: на мобиле сетка в 1 колонку. "
            "CSS-переменные для основного цвета и радиуса. Google Fonts."
        ),
        "checklist": [
            "Hero секция с высотой 100vh и выравниванием по центру",
            "Секция с grid из 3 карточек",
            "Стилизованная форма",
            "Footer",
            "Минимум один медиазапрос",
            "CSS-переменные для цветов",
        ],
        "hint": "Начни с HTML-структуры, потом подключай CSS блок за блоком.",
        "quiz_done_line": (
            "Лендинг — финальный экзамен CSS-модуля. Собери и покажи код. Это будет круто."
        ),
        "win_message": (
            "🏆 Ты прошёл весь CSS-модуль — 18 уроков и настоящий лендинг!\n"
            "<b>Ты уже умеешь:</b> HTML + CSS = полноценная веб-страница с нуля.\n"
            "🚀 Следующий модуль — JavaScript: страница оживёт и начнёт реагировать на действия."
        ),
                "quiz": [
            {
                "q": 'CSS Custom Properties vs SCSS переменные — ключевое отличие?',
                "options": [
                    'SCSS быстрее в рантайме',
                    'CSS-переменные работают в браузере — можно менять через JS и медиазапросы',
                    'SCSS переменные типизированы',
                    'Нет разницы — CSS скомпилирует их в одно',
                ],
                "correct": 1,
                "wrong_hint": 'SCSS-переменные исчезают при компиляции. CSS-переменные живые — меняются через JS и :root.',
            },
            {
                "q": 'Что такое Critical CSS?',
                "options": [
                    'CSS для мобильных',
                    'CSS без !important',
                    'Минимальный CSS для отрисовки контента above the fold — грузится первым',
                    'CSS с высокой специфичностью',
                ],
                "correct": 2,
                "wrong_hint": 'Critical CSS — инлайн-стили для первого экрана. Остальной CSS грузится асинхронно.',
            },
        ],
    },

    # ════════════════════════════════════════════════════════════════════════
    # MODULE 3 · JavaScript · 20 уроков
    # ════════════════════════════════════════════════════════════════════════

    "M3-L1": {
        "title": "let, const, var — переменные",
        "progress_badge": "JS · 1/20",
        "opening": (
            "❓ Зачем три способа объявить переменную?\n"
            "👉 var — устаревший, let — для меняющихся, const — для постоянных. "
            "Сейчас var почти не используют."
        ),
        "hook": "~4 мин: объявишь первые переменные и поймёшь, когда что выбирать — раз и навсегда.",
        "theory": (
            "<code>var</code> — старый способ. Проблемы: function-scope (не block-scope), "
            "hoisting (поднятие), можно переобъявлять. Не используй в новом коде.\n\n"
            "<code>let</code> — блочная область видимости (block-scope). "
            "Можно переназначить: <code>let x = 1; x = 2;</code>.\n\n"
            "<code>const</code> — блочная область, нельзя переназначить. "
            "Обязательно инициализировать сразу. "
            "Для объектов и массивов const — ссылка постоянна, но содержимое изменить можно.\n\n"
            "Типы данных JS:\n"
            "  • string: <code>\"текст\"</code> или <code>'текст'</code> или <code>`шаблон ${var}`</code>.\n"
            "  • number: <code>42</code>, <code>3.14</code>.\n"
            "  • boolean: <code>true</code> / <code>false</code>.\n"
            "  • null: намеренное «ничего».\n"
            "  • undefined: не присвоено значение.\n\n"
            "Вывод: <code>console.log(x);</code> — смотри в DevTools → Console."
        ),
        "nuance": (
            "const с объектом: <code>const obj = { a: 1 }; obj.a = 2;</code> — работает. "
            "<code>obj = {};</code> — ошибка. const блокирует переназначение ссылки, не мутацию."
        ),
        "task": (
            "Открой DevTools → Console или создай .js файл. "
            "Объяви: const с именем (строка), let со счётчиком (число, потом измени), "
            "const с объектом {name, age} и измени одно поле. "
            "Выведи все значения через console.log с пояснением."
        ),
        "checklist": [
            "const со строкой",
            "let с числом, которое переназначается",
            "const с объектом, у которого меняется поле",
            "console.log для каждой переменной",
        ],
        "hint": "const name = 'Alice'; let count = 0; count = 1; console.log(count);",
        "quiz_done_line": (
            "let/const/var в голове — напиши три переменных, поэкспериментируй в консоли. Потом зачёт."
        ),
        "win_message": (
            "🚀 Первый шаг в JavaScript сделан!\n"
            "<b>Ты уже умеешь:</b> объявлять переменные и понимать разницу let/const/var.\n"
            "Дальше — типы данных и typeof: научишься проверять, с чем работаешь."
        ),
                "quiz": [
            {
                "q": 'console.log(typeof NaN) — что выведет?',
                "options": [
                    "'NaN'",
                    "'undefined'",
                    "'number'",
                    "'float'",
                ],
                "correct": 2,
                "wrong_hint": "NaN (Not a Number) имеет тип 'number'. Это один из знаменитых приколов JavaScript.",
            },
            {
                "q": 'const arr = [1, 2, 3]; arr.push(4). Это ошибка?',
                "options": [
                    'Да — const нельзя изменять',
                    'Нет — const запрещает переприсвоение переменной, но не мутацию объекта/массива',
                    'Только если массив примитивный',
                    'Зависит от строгого режима',
                ],
                "correct": 1,
                "wrong_hint": 'const блокирует re-assignment (arr = ...). Мутировать содержимое (push, pop) — можно.',
            },
        ],
    },

    "M3-L2": {
        "title": "Типы данных и typeof",
        "progress_badge": "JS · 2/20",
        "opening": (
            "❓ Почему '5' + 3 = '53', а '5' - 3 = 2 в JavaScript?\n"
            "👉 Неявное приведение типов. Понять типы — значит предсказывать поведение кода."
        ),
        "hook": "~4 мин: разберёшь 7 типов JS и научишься их проверять — осознанно, не наугад.",
        "theory": (
            "Примитивные типы (неизменяемые значения):\n"
            "  • <code>string</code>: 'hello', \"world\", `template`.\n"
            "  • <code>number</code>: 42, 3.14, Infinity, NaN.\n"
            "  • <code>boolean</code>: true / false.\n"
            "  • <code>null</code>: намеренное отсутствие значения.\n"
            "  • <code>undefined</code>: переменная объявлена, но не присвоена.\n"
            "  • <code>symbol</code>: уникальный идентификатор (редко).\n"
            "  • <code>bigint</code>: большие числа (42n).\n\n"
            "Ссылочный тип: <code>object</code> (объекты, массивы, null тоже object — баг).\n\n"
            "<b>typeof:</b> возвращает строку с типом:\n"
            "<code>typeof 42 // 'number'</code>\n"
            "<code>typeof 'hi' // 'string'</code>\n"
            "<code>typeof null // 'object' ← исторический баг</code>\n"
            "<code>typeof undefined // 'undefined'</code>\n\n"
            "<b>Неявное приведение:</b>\n"
            "<code>'5' + 3 = '53'</code> — + приводит к строке при наличии строки.\n"
            "<code>'5' - 3 = 2</code> — - приводит к числу.\n"
            "Проверяй явно: <code>Number('5')</code>, <code>String(42)</code>, <code>Boolean(0)</code>."
        ),
        "nuance": (
            "Falsy-значения в JS: false, 0, '', null, undefined, NaN. "
            "Всё остальное — truthy. "
            "Пустой массив [] и пустой объект {} — truthy!"
        ),
        "task": (
            "Создай переменные каждого примитивного типа. "
            "Для каждой выведи console.log(typeof переменная). "
            "Покажи пример неявного приведения (сложение строки с числом) и явного через Number()."
        ),
        "checklist": [
            "Переменные пяти разных типов (string, number, boolean, null, undefined)",
            "typeof для каждой",
            "Пример неявного приведения типов",
            "Пример явного преобразования через Number или String",
        ],
        "hint": "console.log(typeof null) выведет 'object' — это известный баг JS.",
        "quiz_done_line": (
            "Типы и typeof в голове — напиши переменные и поиграй в консоли. Потом зачёт."
        ),
        "win_message": (
            "🔍 Типы — фундамент JS. Теперь ты знаешь, почему код ведёт себя так, а не иначе.\n"
            "<b>Ты уже умеешь:</b> определять тип и явно приводить значения.\n"
            "Дальше — условия: код начнёт принимать решения."
        ),
                "quiz": [
            {
                "q": 'typeof null возвращает — и почему?',
                "options": [
                    "'null' — специальный тип",
                    "'undefined' — null это пустое значение",
                    "'object' — исторический баг JS не исправленный ради совместимости",
                    "'empty' — пустой тип",
                ],
                "correct": 2,
                "wrong_hint": "typeof null === 'object' — баг из 1995 года. Исправить его уже нельзя — сломается весь старый код.",
            },
            {
                "q": 'Какое значение — truthy (истинное)?',
                "options": [
                    '0',
                    "'' (пустая строка)",
                    'null',
                    '[] (пустой массив)',
                ],
                "correct": 3,
                "wrong_hint": "Пустой массив [] — truthy! Falsy: 0, '', null, undefined, NaN, false.",
            },
        ],
    },

    "M3-L3": {
        "title": "Условия: if/else, switch, тернарный оператор",
        "progress_badge": "JS · 3/20",
        "opening": (
            "❓ Как код принимает решения?\n"
            "👉 Через if/else. Это самая частая конструкция в любой программе."
        ),
        "hook": "~4 мин: напишешь логику «если-то-иначе» — код начнёт думать.",
        "theory": (
            "<b>if/else if/else:</b>\n"
            "<code>if (условие) {\n  // если истина\n} else if (другое) {\n  // ...\n} else {\n  // иначе\n}</code>\n\n"
            "<b>Операторы сравнения:</b>\n"
            "  • <code>===</code> строгое равенство (тип + значение). Используй всегда.\n"
            "  • <code>!==</code> строгое неравенство.\n"
            "  • <code>&gt;</code>, <code>&lt;</code>, <code>&gt;=</code>, <code>&lt;=</code>.\n\n"
            "<b>Логические операторы:</b>\n"
            "  • <code>&amp;&amp;</code> — И: оба условия.\n"
            "  • <code>||</code> — ИЛИ: хотя бы одно.\n"
            "  • <code>!</code> — НЕ: инверсия.\n\n"
            "<b>Тернарный оператор</b> (для одной строки):\n"
            "<code>const label = age >= 18 ? 'взрослый' : 'ребёнок';</code>\n\n"
            "<b>switch</b> — множество вариантов одного значения:\n"
            "<code>switch(day) {\n  case 'Monday': ...; break;\n  default: ...\n}</code>\n\n"
            "<b>Nullish coalescing</b> <code>??</code>: значение если не null/undefined:\n"
            "<code>const name = user.name ?? 'Гость';</code>"
        ),
        "nuance": (
            "== приводит типы: '5' == 5 → true. "
            "=== не приводит: '5' === 5 → false. "
            "Используй только === чтобы избежать сюрпризов."
        ),
        "task": (
            "Функция grade(score): если score >= 90 → 'Отлично', >= 70 → 'Хорошо', >= 50 → 'Удовлетворительно', "
            "иначе → 'Двойка'. Тест с 4 значениями через console.log. "
            "Напиши то же через тернарный оператор для бинарного случая (>=70 ? 'Зачёт' : 'Незачёт')."
        ),
        "checklist": [
            "Функция с if/else if/else и 4 ветками",
            "Правильно использует === для сравнения",
            "Тернарный оператор для бинарного условия",
            "console.log с тестами",
        ],
        "hint": "function grade(score) { if (score >= 90) return 'Отлично'; ... }",
        "quiz_done_line": (
            "Условия в голове — напиши grade() и протестируй в консоли. Потом зачёт."
        ),
        "win_message": (
            "🧠 Код умеет принимать решения — это и есть программирование.\n"
            "<b>Ты уже умеешь:</b> писать ветвистую логику и тернарные выражения.\n"
            "Дальше — функции: многократно используемые блоки кода."
        ),
                "quiz": [
            {
                "q": "console.log('5' + 3) — что выведет?",
                "options": [
                    '8',
                    "'53'",
                    'NaN',
                    'TypeError',
                ],
                "correct": 1,
                "wrong_hint": "Оператор + с строкой — конкатенация. '5' + 3 → '53'. Но '5' - 3 → 2 (число).",
            },
            {
                "q": 'console.log(0 == false) — что выведет?',
                "options": [
                    'false',
                    'true',
                    'TypeError',
                    'undefined',
                ],
                "correct": 1,
                "wrong_hint": '== с принуждением типов: false → 0, 0 == 0 → true. Вот зачем всегда использовать ===.',
            },
        ],
    },

    "M3-L4": {
        "title": "Функции: declaration, expression, arrow",
        "progress_badge": "JS · 4/20",
        "opening": (
            "❓ Зачем выносить код в функцию, если можно написать подряд?\n"
            "👉 Чтобы не повторяться. Одна функция — одна задача — многократный вызов."
        ),
        "hook": "~5 мин: напишешь функцию тремя способами и поймёшь, когда какой выбирать.",
        "theory": (
            "<b>Function Declaration:</b>\n"
            "<code>function greet(name) {\n  return 'Привет, ' + name;\n}</code>\n"
            "Поднимается (hoisting) — можно вызвать до объявления.\n\n"
            "<b>Function Expression:</b>\n"
            "<code>const greet = function(name) {\n  return 'Привет, ' + name;\n};</code>\n"
            "Не поднимается. Часто используется для коллбэков.\n\n"
            "<b>Arrow Function (ES6+):</b>\n"
            "<code>const greet = (name) => 'Привет, ' + name;</code>\n"
            "Кратко. Без скобок вокруг одного параметра: <code>name => ...</code>.\n"
            "Без фигурных скобок — неявный return.\n\n"
            "<b>Параметры и аргументы:</b>\n"
            "  • Значение по умолчанию: <code>function greet(name = 'Мир') {...}</code>\n"
            "  • Rest: <code>function sum(...nums) { return nums.reduce((a,b) => a+b, 0); }</code>\n\n"
            "<b>Чистые функции</b> — нет побочных эффектов, не меняют внешние переменные. "
            "Одинаковый вход → одинаковый выход."
        ),
        "nuance": (
            "Arrow function не имеет своего <code>this</code> — использует this из окружающего контекста. "
            "Это важно в объектах и классах. Для методов объекта лучше обычная функция."
        ),
        "task": (
            "Напиши функцию calculate(a, b, op) тремя способами (declaration, expression, arrow). "
            "op может быть '+', '-', '*', '/'. "
            "Используй switch или object-lookup для выбора операции. "
            "Протестируй каждую."
        ),
        "checklist": [
            "Function declaration версия",
            "Function expression версия",
            "Arrow function версия",
            "switch или object с операциями",
            "Тесты через console.log",
        ],
        "hint": "const ops = { '+': (a,b) => a+b, '-': (a,b) => a-b }; ops[op](a,b);",
        "quiz_done_line": (
            "Три формы функций в руках — напиши calculate() и протестируй. Потом зачёт."
        ),
        "win_message": (
            "⚙️ Функции — главный инструмент переиспользования кода.\n"
            "<b>Ты уже умеешь:</b> писать функции тремя способами и выбирать нужный.\n"
            "Дальше — массивы: коллекции данных и работа с ними."
        ),
                "quiz": [
            {
                "q": 'Function Declaration доступна до места объявления в коде?',
                "options": [
                    'Нет — нужно объявить перед вызовом',
                    'Да — hoisting поднимает объявление функции в начало области видимости',
                    'Только в строгом режиме',
                    'Только если функция не принимает аргументов',
                ],
                "correct": 1,
                "wrong_hint": 'Hoisting: Function Declaration поднимается целиком. Function Expression (const fn = ...) — нет.',
            },
            {
                "q": 'const fn = x => x * 2. Что вернёт fn(5)?',
                "options": [
                    'undefined',
                    'x * 2 (строка)',
                    '10',
                    'NaN',
                ],
                "correct": 2,
                "wrong_hint": 'Стрелочная функция с телом без {} возвращает выражение неявно (implicit return).',
            },
        ],
    },

    "M3-L5": {
        "title": "Массивы: создание и базовые методы",
        "progress_badge": "JS · 5/20",
        "opening": (
            "❓ Как хранить список из 100 имён без 100 переменных?\n"
            "👉 Массив. Один массив — любое количество значений под одним именем."
        ),
        "hook": "~4 мин: создашь массив и научишься добавлять, удалять и находить элементы.",
        "theory": (
            "<code>const arr = [1, 'hi', true, null];</code> — разные типы в одном.\n\n"
            "<b>Базовые операции:</b>\n"
            "  • <code>arr[0]</code> — первый элемент (индекс с 0).\n"
            "  • <code>arr.length</code> — количество элементов.\n\n"
            "<b>Добавление/удаление:</b>\n"
            "  • <code>arr.push(4)</code> — добавить в конец.\n"
            "  • <code>arr.pop()</code> — удалить из конца (возвращает удалённый).\n"
            "  • <code>arr.unshift(0)</code> — добавить в начало.\n"
            "  • <code>arr.shift()</code> — удалить из начала.\n"
            "  • <code>arr.splice(1, 2)</code> — удалить 2 элемента с позиции 1.\n\n"
            "<b>Поиск:</b>\n"
            "  • <code>arr.indexOf(val)</code> — индекс первого вхождения (-1 если нет).\n"
            "  • <code>arr.includes(val)</code> → boolean.\n"
            "  • <code>arr.find(x => x > 3)</code> — первый подходящий.\n\n"
            "<b>Склейка и копия:</b>\n"
            "  • <code>arr.slice(1, 3)</code> — подмассив без мутации.\n"
            "  • <code>[...arr]</code> — поверхностная копия (spread).\n"
            "  • <code>arr1.concat(arr2)</code> или <code>[...arr1, ...arr2]</code>."
        ),
        "nuance": (
            "push/pop/splice мутируют исходный массив. "
            "slice/concat/spread — нет, возвращают новый. "
            "Для предсказуемого кода предпочтительны немутирующие методы."
        ),
        "task": (
            "Массив из 5 фруктов. push нового, pop последнего, найди indexOf('банан'), "
            "проверь includes(), сделай slice первых 3. "
            "Выведи результат каждой операции."
        ),
        "checklist": [
            "Массив из 5 элементов",
            "push и pop с выводом",
            "indexOf и includes",
            "slice с выводом нового массива",
        ],
        "hint": "const fruits = ['яблоко','банан','груша','слива','вишня'];",
        "quiz_done_line": (
            "Базовые методы массивов в руках — напиши и протестируй. Потом зачёт."
        ),
        "win_message": (
            "📚 Массив — главная структура данных в JS.\n"
            "<b>Ты уже умеешь:</b> создавать массивы и управлять их содержимым.\n"
            "Дальше — map, filter, reduce: функциональный подход к данным."
        ),
                "quiz": [
            {
                "q": '[1, 2, 3].find(x => x > 1) — что вернёт?',
                "options": [
                    '[2, 3]',
                    'true',
                    '2',
                    '1',
                ],
                "correct": 2,
                "wrong_hint": 'find — возвращает первый элемент, удовлетворяющий условию. Хочешь все — используй filter.',
            },
            {
                "q": 'Разница между slice и splice?',
                "options": [
                    'Нет разницы',
                    'slice возвращает копию не мутируя оригинал; splice мутирует оригинал',
                    'splice работает только с числами',
                    'slice удаляет элементы, splice — нет',
                ],
                "correct": 1,
                "wrong_hint": 'slice — чистый (не мутирует), splice — изменяет исходный массив. Частая ловушка на собеседованиях.',
            },
        ],
    },

    "M3-L6": {
        "title": "map, filter, reduce, forEach",
        "progress_badge": "JS · 6/20",
        "opening": (
            "❓ Как обработать каждый элемент массива без цикла for?\n"
            "👉 Функциональные методы: map, filter, reduce. Это современный JS."
        ),
        "hook": "~5 мин: превратишь данные одним выражением — как в реальных проектах.",
        "theory": (
            "<b>forEach</b> — выполнить функцию для каждого элемента (нет return):\n"
            "<code>arr.forEach(item => console.log(item));</code>\n\n"
            "<b>map</b> — преобразовать каждый элемент, вернуть новый массив:\n"
            "<code>const doubled = [1,2,3].map(x => x * 2); // [2,4,6]</code>\n\n"
            "<b>filter</b> — оставить элементы, прошедшие проверку:\n"
            "<code>const evens = [1,2,3,4].filter(x => x % 2 === 0); // [2,4]</code>\n\n"
            "<b>reduce</b> — свернуть массив в одно значение:\n"
            "<code>const sum = [1,2,3].reduce((acc, cur) => acc + cur, 0); // 6</code>\n"
            "acc — накопитель, cur — текущий, 0 — начальное значение.\n\n"
            "<b>Цепочки методов:</b>\n"
            "<code>const result = users\n"
            "  .filter(u => u.age >= 18)\n"
            "  .map(u => u.name)\n"
            "  .sort();</code>\n\n"
            "<b>Дополнительно:</b> <code>some()</code> — хотя бы один подходит, "
            "<code>every()</code> — все подходят, <code>flat()</code> — разворачивает вложенные массивы."
        ),
        "nuance": (
            "map возвращает массив той же длины. filter — меньше или равно. "
            "reduce — любой тип. "
            "Не используй forEach там где нужен map/filter — forEach ничего не возвращает."
        ),
        "task": (
            "Массив объектов [{name, score}] из 5 студентов. "
            "filter: только сдавшие (score >= 60). "
            "map: массив имён сдавших. "
            "reduce: средний балл всех. "
            "Выведи каждый результат."
        ),
        "checklist": [
            "Массив из 5 объектов с name и score",
            "filter для score >= 60",
            "map для получения имён",
            "reduce для среднего балла",
            "console.log результатов",
        ],
        "hint": "reduce для среднего: arr.reduce((acc, s) => acc + s.score, 0) / arr.length",
        "quiz_done_line": (
            "map/filter/reduce в руках — напиши студентов и обработай. Потом зачёт."
        ),
        "win_message": (
            "🔧 Функциональные методы — основа современного JS.\n"
            "<b>Ты уже умеешь:</b> преобразовывать данные выразительно без циклов.\n"
            "Дальше — объекты: хранить структурированные данные."
        ),
                "quiz": [
            {
                "q": '[1, 2, 3].reduce((acc, x) => acc + x, 0) — что вернёт?',
                "options": [
                    '[0, 1, 3, 6]',
                    '0',
                    '6',
                    '[1, 2, 3, 0]',
                ],
                "correct": 2,
                "wrong_hint": 'reduce аккумулирует: 0+1=1, 1+2=3, 3+3=6. Второй аргумент (0) — начальное значение.',
            },
            {
                "q": 'Разница между map и forEach?',
                "options": [
                    'forEach быстрее',
                    'map мутирует исходный массив',
                    'map возвращает новый массив; forEach возвращает undefined',
                    'forEach принимает только одну функцию',
                ],
                "correct": 2,
                "wrong_hint": 'map → новый массив с результатами. forEach → только побочные эффекты, возвращает undefined.',
            },
        ],
    },

    "M3-L7": {
        "title": "Объекты: создание, деструктуризация, spread",
        "progress_badge": "JS · 7/20",
        "opening": (
            "❓ Как хранить связанные данные (имя + возраст + город) без трёх отдельных переменных?\n"
            "👉 Объект. Ключ-значение — базовая структура любого API."
        ),
        "hook": "~5 мин: создашь объект, деструктуришь его и научишься копировать без мутации.",
        "theory": (
            "<b>Создание объекта:</b>\n"
            "<code>const user = {\n  name: 'Alice',\n  age: 25,\n  greet() { return `Привет, ${this.name}`; }\n};</code>\n\n"
            "<b>Доступ:</b>\n"
            "  • Точка: <code>user.name</code>.\n"
            "  • Скобки: <code>user['name']</code> — динамический ключ.\n\n"
            "<b>Деструктуризация:</b>\n"
            "<code>const { name, age } = user;</code>\n"
            "<code>const { name: firstName } = user;</code> — переименование.\n"
            "<code>const { city = 'Москва' } = user;</code> — значение по умолчанию.\n\n"
            "<b>Spread и копия:</b>\n"
            "<code>const updated = { ...user, age: 26 };</code> — новый объект с изменённым полем.\n\n"
            "<b>Методы Object:</b>\n"
            "  • <code>Object.keys(obj)</code> — массив ключей.\n"
            "  • <code>Object.values(obj)</code> — массив значений.\n"
            "  • <code>Object.entries(obj)</code> — массив [ключ, значение].\n"
            "  • <code>Object.assign({}, obj)</code> — мелкое копирование."
        ),
        "nuance": (
            "Spread-копия поверхностная: вложенные объекты остаются ссылками. "
            "Для глубокой копии: structuredClone(obj) или JSON.parse(JSON.stringify(obj)) — с ограничениями."
        ),
        "task": (
            "Объект product с 5 полями (name, price, category, inStock, rating). "
            "Деструктурируй name и price с переименованием. "
            "Создай discounted через spread с изменённым price (-10%). "
            "Выведи Object.keys, Object.values, Object.entries."
        ),
        "checklist": [
            "Объект с 5 полями",
            "Деструктуризация с переименованием",
            "Spread для создания копии с изменением",
            "Object.keys/values/entries",
        ],
        "hint": "const { price: originalPrice } = product; — деструктуризация с переименованием.",
        "quiz_done_line": (
            "Объекты, деструктуризация, spread — напиши product и обработай. Потом зачёт."
        ),
        "win_message": (
            "📦 Объекты — фундамент любых данных в JS (JSON, API, state).\n"
            "<b>Ты уже умеешь:</b> создавать, копировать и деструктурировать объекты.\n"
            "Дальше — циклы: перебор коллекций."
        ),
                "quiz": [
            {
                "q": 'const [a, , b] = [1, 2, 3]. Чему равно b?',
                "options": [
                    '1',
                    '2',
                    '3',
                    'undefined',
                ],
                "correct": 2,
                "wrong_hint": 'Пропущенная запятая в деструктуризации пропускает элемент. a=1, пропуск=2, b=3.',
            },
            {
                "q": 'const copy = { ...obj } — это глубокая копия?',
                "options": [
                    'Да — spread копирует всё',
                    'Нет — только поверхностная; вложенные объекты копируются по ссылке',
                    'Зависит от содержимого объекта',
                    'Да, но только в строгом режиме',
                ],
                "correct": 1,
                "wrong_hint": 'Spread — shallow copy. obj.nested в копии указывает на тот же объект что в оригинале.',
            },
        ],
    },

    "M3-L8": {
        "title": "Циклы: for, while, for...of, for...in",
        "progress_badge": "JS · 8/20",
        "opening": (
            "❓ Когда нужен цикл вместо map/filter?\n"
            "👉 Когда нужно выйти раньше времени, или логика сложнее трансформации."
        ),
        "hook": "~4 мин: разберёшь все типы циклов и поймёшь, когда какой уместен.",
        "theory": (
            "<b>for</b> — классический с счётчиком:\n"
            "<code>for (let i = 0; i < arr.length; i++) { arr[i] }</code>\n\n"
            "<b>while</b> — пока условие истинно:\n"
            "<code>while (condition) { ... }</code>\n"
            "<code>do { ... } while (condition);</code> — выполняет хотя бы раз.\n\n"
            "<b>for...of</b> — перебор значений итерируемых (массив, строка, Set, Map):\n"
            "<code>for (const item of arr) { console.log(item); }</code>\n\n"
            "<b>for...in</b> — перебор ключей объекта:\n"
            "<code>for (const key in obj) { obj[key] }</code>\n"
            "Осторожно: перебирает также унаследованные свойства.\n\n"
            "<b>break</b> — выход из цикла.\n"
            "<b>continue</b> — пропустить текущую итерацию.\n\n"
            "<b>Когда что:</b>\n"
            "  • Трансформация массива → map/filter/reduce.\n"
            "  • Перебор массива → for...of.\n"
            "  • Перебор ключей объекта → for...in или Object.keys().\n"
            "  • Счётчик или нестандартный шаг → for."
        ),
        "nuance": (
            "for...in на массиве — плохая практика: перебирает строковые индексы + прототип. "
            "Для массивов — for...of или forEach."
        ),
        "task": (
            "for: числа от 1 до 10, вывести только чётные (continue). "
            "for...of: перебрать массив городов, вывести каждый. "
            "while: угадывание числа (фиксированный «random», выход через break если угадал). "
            "for...in: вывести все ключ-значение объекта."
        ),
        "checklist": [
            "for с continue для чётных",
            "for...of для массива",
            "while или do-while с break",
            "for...in для объекта",
        ],
        "hint": "for (let i = 1; i <= 10; i++) { if (i % 2 !== 0) continue; console.log(i); }",
        "quiz_done_line": (
            "Четыре типа циклов в голове — напиши все четыре примера. Потом зачёт."
        ),
        "win_message": (
            "🔁 Циклы — управление потоком выполнения.\n"
            "<b>Ты уже умеешь:</b> выбирать правильный цикл под задачу.\n"
            "Дальше — DOM: JavaScript начнёт управлять страницей."
        ),
                "quiz": [
            {
                "q": 'for (let i=0; i<3; i++) { if (i===1) continue; console.log(i); } — что выведет?',
                "options": [
                    '0 1 2',
                    '0 2',
                    '1',
                    '0 1',
                ],
                "correct": 1,
                "wrong_hint": 'continue пропускает текущую итерацию. i=1 пропускается, выводятся 0 и 2.',
            },
            {
                "q": 'for...of vs for...in — принципиальная разница?',
                "options": [
                    'Нет разницы',
                    'for...in перебирает ключи объекта; for...of — значения итерируемого',
                    'for...in работает только с массивами',
                    'for...of работает только с числами',
                ],
                "correct": 1,
                "wrong_hint": "for...in → ключи ('0','1','length'...). for...of → значения (1,2,3...). Не путай при переборе массивов.",
            },
        ],
    },

    "M3-L9": {
        "title": "DOM: querySelector и работа с элементами",
        "progress_badge": "JS · 9/20",
        "opening": (
            "❓ Как JavaScript «видит» HTML-страницу?\n"
            "👉 Через DOM (Document Object Model) — дерево объектов. querySelector находит узел в этом дереве."
        ),
        "hook": "~5 мин: найдёшь элементы на странице и прочитаешь их свойства — JS начнёт взаимодействовать с HTML.",
        "theory": (
            "DOM — программное представление HTML. Каждый тег — объект с методами и свойствами.\n\n"
            "<b>Поиск элементов:</b>\n"
            "  • <code>document.querySelector('.card')</code> — первый подходящий.\n"
            "  • <code>document.querySelectorAll('li')</code> — NodeList всех подходящих.\n"
            "  • <code>document.getElementById('header')</code> — по id.\n"
            "  • <code>el.closest('.parent')</code> — ближайший предок.\n"
            "  • <code>el.querySelector('.child')</code> — поиск внутри элемента.\n\n"
            "<b>Чтение свойств:</b>\n"
            "  • <code>el.textContent</code> — текст (без HTML).\n"
            "  • <code>el.innerHTML</code> — HTML внутри.\n"
            "  • <code>el.getAttribute('href')</code> — значение атрибута.\n"
            "  • <code>el.dataset.id</code> — data-атрибут (data-id=\"5\").\n"
            "  • <code>el.style.color</code> — inline-стиль.\n"
            "  • <code>el.offsetWidth</code>, <code>el.getBoundingClientRect()</code> — размеры и позиция.\n\n"
            "<b>Обход дерева:</b>\n"
            "  • <code>el.parentElement</code>, <code>el.children</code>, <code>el.nextElementSibling</code>."
        ),
        "nuance": (
            "querySelectorAll возвращает NodeList — не массив. "
            "Для методов массива: <code>Array.from(nodeList)</code> или <code>[...nodeList]</code>."
        ),
        "task": (
            "HTML: ul с 5 li (разные data-id). "
            "JS: найди через querySelectorAll, конвертируй в массив, "
            "выведи textContent каждого и dataset.id. "
            "Найди второй li через querySelector и выведи его offsetWidth."
        ),
        "checklist": [
            "querySelectorAll для li",
            "Array.from или spread для конвертации NodeList",
            "Вывод textContent и dataset.id каждого",
            "querySelector для конкретного элемента",
        ],
        "hint": "const items = [...document.querySelectorAll('li')]; items.forEach(li => console.log(li.textContent));",
        "quiz_done_line": (
            "DOM-поиск в руках — напиши HTML с li и JS с querySelector. Потом зачёт."
        ),
        "win_message": (
            "🌳 Теперь JS видит страницу.\n"
            "<b>Ты уже умеешь:</b> находить любой элемент и читать его свойства.\n"
            "Дальше — изменение DOM: JS начнёт менять страницу."
        ),
                "quiz": [
            {
                "q": "querySelector('.btn') vs querySelectorAll('.btn') — разница?",
                "options": [
                    'Нет разницы',
                    'querySelector — первый элемент; querySelectorAll — NodeList всех совпадений',
                    'querySelectorAll работает только с классами',
                    'querySelector быстрее на 10x',
                ],
                "correct": 1,
                "wrong_hint": 'querySelector → первый найденный или null. querySelectorAll → NodeList (возможно пустой).',
            },
            {
                "q": "Ты создал элемент через document.createElement('div'). Пользователь его видит?",
                "options": [
                    'Да, сразу',
                    'Да, через setTimeout',
                    'Нет — элемент нужно добавить в DOM через append/appendChild',
                    'Нет — createElement устарел',
                ],
                "correct": 2,
                "wrong_hint": 'createElement создаёт элемент в памяти. Чтобы он появился на странице — добавь его в DOM.',
            },
        ],
    },

    "M3-L10": {
        "title": "DOM: изменение контента и стилей",
        "progress_badge": "JS · 10/20",
        "opening": (
            "❓ Как обновить текст на странице без перезагрузки?\n"
            "👉 Изменить textContent или innerHTML через JS — страница обновится мгновенно."
        ),
        "hook": "~5 мин: напишешь JS, который меняет страницу — вот где начинается настоящий интерактив.",
        "theory": (
            "<b>Изменение контента:</b>\n"
            "  • <code>el.textContent = 'Новый текст';</code> — безопасно (HTML экранируется).\n"
            "  • <code>el.innerHTML = '&lt;b&gt;Жирный&lt;/b&gt;';</code> — парсит HTML (XSS-опасно с user input).\n\n"
            "<b>Изменение атрибутов:</b>\n"
            "  • <code>el.setAttribute('href', '/new');</code>\n"
            "  • <code>el.removeAttribute('disabled');</code>\n"
            "  • <code>img.src = 'new.jpg';</code> — напрямую.\n\n"
            "<b>Изменение классов (classList):</b>\n"
            "  • <code>el.classList.add('active');</code>\n"
            "  • <code>el.classList.remove('active');</code>\n"
            "  • <code>el.classList.toggle('active');</code> — добавить/убрать.\n"
            "  • <code>el.classList.contains('active');</code> → boolean.\n\n"
            "<b>Изменение стилей:</b>\n"
            "  • <code>el.style.color = 'red';</code> — inline стиль.\n"
            "  • Лучше через classList: менять классы, стили в CSS.\n\n"
            "<b>Создание и вставка:</b>\n"
            "  • <code>const div = document.createElement('div');</code>\n"
            "  • <code>div.textContent = 'Новый';</code>\n"
            "  • <code>parent.append(div);</code> — вставить в конец.\n"
            "  • <code>parent.prepend(div);</code> — в начало.\n"
            "  • <code>el.remove();</code> — удалить элемент."
        ),
        "nuance": (
            "classList.toggle('dark-mode') на body — простейшая тёмная тема одной строкой."
        ),
        "task": (
            "Кнопка 'Переключить тему' — по клику toggle класс dark-mode на body. "
            "CSS: body.dark-mode { background: #1a1a1a; color: #eee; }. "
            "Счётчик: кнопки + и -, отображение числа в span, обновление textContent."
        ),
        "checklist": [
            "Кнопка с toggle dark-mode на body",
            "CSS для dark-mode",
            "Счётчик с двумя кнопками",
            "textContent обновляется при каждом нажатии",
        ],
        "hint": "btn.addEventListener('click', () => document.body.classList.toggle('dark-mode'));",
        "quiz_done_line": (
            "DOM-изменения в руках — собери тему и счётчик. Потом зачёт."
        ),
        "win_message": (
            "✨ Страница реагирует на JS — это и есть интерактивность.\n"
            "<b>Ты уже умеешь:</b> изменять контент, классы и стили через JS.\n"
            "Дальше — события: реагирование на действия пользователя."
        ),
                "quiz": [
            {
                "q": 'Почему textContent безопаснее innerHTML при вставке пользовательских данных?',
                "options": [
                    'textContent быстрее',
                    'innerHTML не поддерживает кириллицу',
                    'innerHTML интерпретирует HTML-теги — возможна XSS-атака через вредоносный скрипт',
                    'textContent поддерживает больше браузеров',
                ],
                "correct": 2,
                "wrong_hint": "innerHTML('<script>alert(1)</script>') выполнит скрипт. textContent вставит его как текст безопасно.",
            },
            {
                "q": "classList.toggle('active') — что делает?",
                "options": [
                    'Всегда добавляет класс active',
                    'Всегда удаляет класс active',
                    'Добавляет если нет, удаляет если есть',
                    'Проверяет наличие класса и возвращает true/false',
                ],
                "correct": 2,
                "wrong_hint": 'toggle — переключатель. Удобно для бургер-меню, аккордеонов, темёмной темы.',
            },
        ],
    },

    "M3-L11": {
        "title": "События: addEventListener, делегирование",
        "progress_badge": "JS · 11/20",
        "opening": (
            "❓ Как обработать клик на 100 кнопках без 100 обработчиков?\n"
            "👉 Делегирование событий — один обработчик на родителя."
        ),
        "hook": "~5 мин: освоишь события и делегирование — навык, который используется в каждом проекте.",
        "theory": (
            "<b>addEventListener:</b>\n"
            "<code>el.addEventListener('click', handler);</code>\n"
            "Виды событий: click, dblclick, mouseover, mouseout, mousedown, mouseup,\n"
            "keydown, keyup, keypress, focus, blur, change, input, submit.\n\n"
            "<b>Объект события (event):</b>\n"
            "  • <code>event.target</code> — элемент, на котором произошло событие.\n"
            "  • <code>event.currentTarget</code> — элемент с обработчиком.\n"
            "  • <code>event.preventDefault()</code> — отменить действие по умолчанию (переход ссылки, отправку формы).\n"
            "  • <code>event.stopPropagation()</code> — остановить всплытие.\n"
            "  • <code>event.key</code> — нажатая клавиша.\n\n"
            "<b>Всплытие (bubbling):</b> событие идёт от target вверх по дереву. "
            "Родитель тоже получит click, если не остановить.\n\n"
            "<b>Делегирование:</b> слушатель на родителе, проверяем event.target:\n"
            "<code>ul.addEventListener('click', e => {\n"
            "  if (e.target.matches('li')) { ... }\n"
            "});</code>\n"
            "Работает даже для динамически добавленных элементов."
        ),
        "nuance": (
            "removeEventListener требует ту же ссылку на функцию. "
            "Анонимная стрелка — нельзя удалить. "
            "Если обработчик нужно убрать — именуй функцию."
        ),
        "task": (
            "Список задач (ul с li): кнопки добавить и удалить. "
            "Делегирование: один click на ul, по клику на li — toggle класс 'done' (зачёркнутый). "
            "Форма: preventDefault, взять значение input и добавить новый li в список."
        ),
        "checklist": [
            "addEventListener на ul для делегирования",
            "e.target.matches('li') для фильтрации",
            "toggle класса done по клику на li",
            "Форма с preventDefault и добавлением li",
        ],
        "hint": "ul.addEventListener('click', e => { if(e.target.tagName === 'LI') e.target.classList.toggle('done'); });",
        "quiz_done_line": (
            "Делегирование в руках — собери todo-список. Потом зачёт."
        ),
        "win_message": (
            "🎯 Делегирование — признак опытного JS-разработчика.\n"
            "<b>Ты уже умеешь:</b> обрабатывать события эффективно, включая динамические элементы.\n"
            "Дальше — fetch и промисы: работа с внешними данными."
        ),
                "quiz": [
            {
                "q": 'Что такое всплытие событий (event bubbling)?',
                "options": [
                    'Событие перехватывается только на целевом элементе',
                    'Событие возникает на цели, затем поднимается к родителям вплоть до document',
                    'Событие спускается от document к цели',
                    'Событие дублируется на всех элементах',
                ],
                "correct": 1,
                "wrong_hint": 'Bubbling: click на кнопке → всплывает к div → body → document. На этом строится делегирование.',
            },
            {
                "q": 'event.stopPropagation() — что делает?',
                "options": [
                    'Отменяет стандартное действие браузера (переход, submit)',
                    'Останавливает всплытие события к родителям',
                    'Удаляет обработчик события',
                    'Отключает все события на элементе',
                ],
                "correct": 1,
                "wrong_hint": 'stopPropagation — стоп всплытию. preventDefault — отмена действия браузера. Разные вещи.',
            },
        ],
    },

    "M3-L12": {
        "title": "Создание элементов и работа с формами",
        "progress_badge": "JS · 12/20",
        "opening": (
            "❓ Как рендерить список из 100 элементов не записывая HTML вручную?\n"
            "👉 createElement в цикле или innerHTML с шаблонными строками."
        ),
        "hook": "~5 мин: построишь список из данных программно — это основа любого динамического UI.",
        "theory": (
            "<b>Создание и вставка:</b>\n"
            "<code>const li = document.createElement('li');\n"
            "li.textContent = 'Пункт';\n"
            "li.classList.add('item');\n"
            "ul.append(li);</code>\n\n"
            "<b>insertAdjacentHTML</b> — вставка HTML-строки:\n"
            "<code>el.insertAdjacentHTML('beforeend', '&lt;li&gt;Новый&lt;/li&gt;');</code>\n"
            "Позиции: beforebegin, afterbegin, beforeend, afterend.\n\n"
            "<b>Шаблонные строки для рендеринга:</b>\n"
            "<code>ul.innerHTML = items.map(i => `&lt;li&gt;${i.name}&lt;/li&gt;`).join('');</code>\n\n"
            "<b>Работа с формами:</b>\n"
            "  • <code>form.elements['email'].value</code> — значение поля.\n"
            "  • <code>input.value</code> — текущее значение.\n"
            "  • <code>input.value = ''</code> — очистить.\n"
            "  • <code>checkbox.checked</code> → boolean.\n"
            "  • <code>select.value</code> — выбранный option.\n"
            "  • <code>new FormData(form)</code> — все данные формы сразу.\n\n"
            "<b>Валидация:</b>\n"
            "  • <code>input.validity.valid</code> → boolean.\n"
            "  • <code>form.reportValidity()</code> — показать браузерные ошибки."
        ),
        "nuance": (
            "innerHTML = '...' сбрасывает все слушатели событий на дочерних элементах. "
            "Для частичного обновления — используй createElement или insertAdjacentHTML."
        ),
        "task": (
            "Массив из 5 объектов {title, done: bool}. "
            "Рендер через innerHTML + map (шаблонные строки). "
            "Форма добавления нового: берёт значение input, добавляет в массив, перерендеривает. "
            "Очистка input после добавления."
        ),
        "checklist": [
            "Рендер через innerHTML с map и шаблонными строками",
            "Форма с addEventListener на submit",
            "preventDefault на submit",
            "Добавление в массив и повторный рендер",
            "Очистка input после добавления",
        ],
        "hint": "<code>function render() { ul.innerHTML = todos.map(t =&gt; `&lt;li&gt;${t.title}&lt;/li&gt;`).join(''); }</code>",
        "quiz_done_line": (
            "Рендер из данных и форма — собери динамический список. Потом зачёт."
        ),
        "win_message": (
            "🏗 Ты рендеришь UI из данных — это основа React, Vue и любого фреймворка.\n"
            "<b>Ты уже умеешь:</b> строить интерфейс программно из массива объектов.\n"
            "Дальше — fetch: данные начнут приходить с реального сервера."
        ),
                "quiz": [
            {
                "q": 'Ты добавляешь 1000 <li> в цикле через appendChild на каждой итерации. Почему тормозит?',
                "options": [
                    'JavaScript медленный для больших чисел',
                    'Каждый appendChild вызывает reflow DOM — нужно использовать DocumentFragment',
                    'Нужен Web Worker',
                    '1000 элементов — это нормально, не тормозит',
                ],
                "correct": 1,
                "wrong_hint": 'Собери все узлы в DocumentFragment, потом одним append добавь в DOM — один reflow вместо 1000.',
            },
            {
                "q": 'Что такое debounce обработчика?',
                "options": [
                    'Немедленное срабатывание при первом событии',
                    'Функция вызывается не чаще 1 раза в N мс (throttle)',
                    'Функция вызывается через N мс после последнего события в серии',
                    'Обработчик удаляется после первого вызова',
                ],
                "correct": 2,
                "wrong_hint": 'Debounce: ждёт паузы. Идеально для поля поиска — запрос отправляется когда пользователь перестал печатать.',
            },
        ],
    },

    "M3-L13": {
        "title": "Промисы и fetch API",
        "progress_badge": "JS · 13/20",
        "opening": (
            "❓ Почему нельзя просто дождаться ответа сервера, как обычного результата функции?\n"
            "👉 Потому что JS однопоточный — ожидание заблокирует всё. Промисы решают это элегантно."
        ),
        "hook": "~5 мин: загрузишь данные с реального API и отрендеришь на страницу — это самое важное в веб-разработке.",
        "theory": (
            "<b>Promise</b> — обещание результата в будущем:\n"
            "<code>const p = new Promise((resolve, reject) => {\n"
            "  // асинхронная операция\n"
            "  resolve(data); // успех\n"
            "  reject(error); // ошибка\n"
            "});\n"
            "p.then(data => ...).catch(err => ...).finally(() => ...);</code>\n\n"
            "Состояния: pending → fulfilled | rejected.\n\n"
            "<b>fetch API</b>:\n"
            "<code>fetch('https://api.example.com/data')\n"
            "  .then(response => {\n"
            "    if (!response.ok) throw new Error(response.status);\n"
            "    return response.json();\n"
            "  })\n"
            "  .then(data => console.log(data))\n"
            "  .catch(err => console.error(err));</code>\n\n"
            "<b>Promise.all</b> — дождаться нескольких промисов:\n"
            "<code>Promise.all([fetch(url1), fetch(url2)]).then(([r1, r2]) => ...);</code>\n\n"
            "Бесплатные тестовые API: <code>https://jsonplaceholder.typicode.com/posts</code>"
        ),
        "nuance": (
            "fetch не бросает ошибку при 404 или 500 — только при сетевой ошибке. "
            "Проверяй response.ok (или response.status) вручную."
        ),
        "task": (
            "Загрузи список постов с https://jsonplaceholder.typicode.com/posts?_limit=5. "
            "Обработай через .then/.catch. "
            "Отрендери заголовки (title) в ul на странице. "
            "Пока данные грузятся — показывай 'Загрузка...', после — скрывай."
        ),
        "checklist": [
            "fetch к jsonplaceholder",
            "Проверка response.ok",
            "response.json() и рендер в ul",
            "Состояние загрузки (loading/loaded)",
            ".catch с выводом ошибки",
        ],
        "hint": "fetch('https://jsonplaceholder.typicode.com/posts?_limit=5').then(r => r.json()).then(posts => ...)",
        "quiz_done_line": (
            "fetch и промисы в руках — загрузи посты и отрендери. Потом зачёт."
        ),
        "win_message": (
            "🌐 Ты умеешь работать с API — это core-навык любого фронтенд-разработчика.\n"
            "<b>Ты уже умеешь:</b> загружать данные с сервера и показывать на странице.\n"
            "Дальше — async/await: тот же fetch, но читается как синхронный код."
        ),
                "quiz": [
            {
                "q": "fetch('/api/data') вернул статус 404. Попадём в .catch()?",
                "options": [
                    'Да — 404 это ошибка',
                    'Нет — .catch() только для сетевых сбоев; нужно проверять response.ok',
                    'Зависит от сервера',
                    "Да, если добавить { mode: 'cors' }",
                ],
                "correct": 1,
                "wrong_hint": 'fetch отклоняет promise только при сетевой ошибке. 404/500 — успешный ответ с кодом ошибки.',
            },
            {
                "q": 'Что нужно для POST-запроса с JSON через fetch?',
                "options": [
                    "{ type: 'POST', json: data }",
                    "{ method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }",
                    "fetch.post('/url', data)",
                    "{ method: 'POST', body: data }",
                ],
                "correct": 1,
                "wrong_hint": 'Три ключа: method, Content-Type заголовок и JSON.stringify для body. Без заголовка сервер не поймёт формат.',
            },
        ],
    },

    "M3-L14": {
        "title": "async/await и обработка ошибок",
        "progress_badge": "JS · 14/20",
        "opening": (
            "❓ Почему .then().then().then() — нечитабельно?\n"
            "👉 async/await — тот же промис, но выглядит как синхронный код."
        ),
        "hook": "~4 мин: перепишешь fetch на async/await и поймёшь, почему все так пишут.",
        "theory": (
            "<code>async function loadData() {\n"
            "  try {\n"
            "    const response = await fetch(url);\n"
            "    if (!response.ok) throw new Error(`HTTP ${response.status}`);\n"
            "    const data = await response.json();\n"
            "    return data;\n"
            "  } catch (err) {\n"
            "    console.error('Ошибка:', err);\n"
            "    throw err; // пробрасываем выше если нужно\n"
            "  } finally {\n"
            "    hideLoader();\n"
            "  }\n"
            "}</code>\n\n"
            "  • <code>async</code> перед функцией — она всегда возвращает Promise.\n"
            "  • <code>await</code> — ждёт промис, код приостанавливается (не блокируя браузер).\n"
            "  • await только внутри async-функции.\n\n"
            "<b>Параллельные запросы:</b>\n"
            "<code>const [users, posts] = await Promise.all([\n"
            "  fetch('/api/users').then(r => r.json()),\n"
            "  fetch('/api/posts').then(r => r.json()),\n"
            "]);</code>\n\n"
            "<b>Promise.allSettled</b> — дождаться всех, даже если часть упала.\n"
            "<b>Promise.race</b> — первый выполненный (таймаут для fetch)."
        ),
        "nuance": (
            "Top-level await работает только в ES-модулях (type=\"module\"). "
            "В обычных скриптах оборачивай в async IIFE: "
            "<code>(async () => { ... })();</code>"
        ),
        "task": (
            "Перепиши fetch из прошлого урока на async/await с try/catch/finally. "
            "Добавь параллельную загрузку двух ресурсов через Promise.all "
            "(например posts и users с jsonplaceholder). "
            "В finally — скрыть лоадер."
        ),
        "checklist": [
            "async function с await для fetch",
            "try/catch для обработки ошибок",
            "finally для скрытия лоадера",
            "Promise.all для параллельных запросов",
        ],
        "hint": "async function main() { const [posts, users] = await Promise.all([...]) }",
        "quiz_done_line": (
            "async/await в руках — перепиши и протестируй. Потом зачёт."
        ),
        "win_message": (
            "⚡ async/await — стандарт современного JS для асинхронного кода.\n"
            "<b>Ты уже умеешь:</b> писать асинхронный код, который читается как синхронный.\n"
            "Дальше — JSON и localStorage: данные не будут теряться при перезагрузке."
        ),
                "quiz": [
            {
                "q": 'async функция ВСЕГДА возвращает?',
                "options": [
                    'Значение из return напрямую',
                    'undefined если нет return',
                    'Promise',
                    'Зависит от содержимого функции',
                ],
                "correct": 2,
                "wrong_hint": 'async оборачивает любое возвращаемое значение в Promise.resolve(). Это фундаментальное правило.',
            },
            {
                "q": 'Как выполнить два независимых fetch параллельно с async/await?',
                "options": [
                    'await fetch1(); await fetch2(); — они параллельны',
                    'const [r1, r2] = await Promise.all([fetch1(), fetch2()]);',
                    'async fetch1(); async fetch2();',
                    'Promise.race([fetch1(), fetch2()])',
                ],
                "correct": 1,
                "wrong_hint": 'await fetch1 затем await fetch2 — последовательно. Promise.all — запускает оба сразу, ждёт оба.',
            },
        ],
    },

    "M3-L15": {
        "title": "JSON и localStorage",
        "progress_badge": "JS · 15/20",
        "opening": (
            "❓ Как сохранить данные пользователя между перезагрузками без сервера?\n"
            "👉 localStorage — хранилище в браузере, данные не исчезают при закрытии."
        ),
        "hook": "~4 мин: научишься сохранять и восстанавливать данные приложения — основа офлайн-функциональности.",
        "theory": (
            "<b>JSON:</b>\n"
            "<code>JSON.stringify(obj)</code> — объект → строка.\n"
            "<code>JSON.parse(str)</code> — строка → объект.\n\n"
            "JSON не поддерживает: undefined, функции, Symbol, Date (→ строка). "
            "Числа, строки, булевы, null, массивы, объекты — поддерживает.\n\n"
            "<b>localStorage:</b>\n"
            "  • <code>localStorage.setItem('key', value)</code> — сохранить строку.\n"
            "  • <code>localStorage.getItem('key')</code> — прочитать (или null).\n"
            "  • <code>localStorage.removeItem('key')</code> — удалить.\n"
            "  • <code>localStorage.clear()</code> — очистить всё.\n\n"
            "Хранит только строки — сериализуй через JSON:\n"
            "<code>localStorage.setItem('todos', JSON.stringify(todos));\n"
            "const todos = JSON.parse(localStorage.getItem('todos') ?? '[]');</code>\n\n"
            "<b>sessionStorage</b> — то же, но очищается при закрытии вкладки.\n\n"
            "Лимит: ~5MB. Синхронный (не блокирует, но медленный для больших данных)."
        ),
        "nuance": (
            "localStorage.getItem возвращает null если ключа нет. "
            "JSON.parse(null) — ошибка. Используй nullish coalescing: "
            "<code>JSON.parse(localStorage.getItem('todos') ?? '[]')</code>"
        ),
        "task": (
            "Todo-список с localStorage: загрузка из localStorage при старте, "
            "сохранение при каждом добавлении/удалении, "
            "кнопка 'Очистить всё' (localStorage.clear + рендер). "
            "После перезагрузки страницы — список сохранился."
        ),
        "checklist": [
            "Загрузка из localStorage при init",
            "Сохранение через JSON.stringify при каждом изменении",
            "Кнопка очистки",
            "После перезагрузки данные сохраняются",
        ],
        "hint": "function save() { localStorage.setItem('todos', JSON.stringify(todos)); }",
        "quiz_done_line": (
            "localStorage в руках — добавь персистентность к списку. Потом зачёт."
        ),
        "win_message": (
            "💾 Данные теперь живут между сессиями — без сервера!\n"
            "<b>Ты уже умеешь:</b> персистентно хранить данные в браузере через JSON + localStorage.\n"
            "Дальше — классы ES6: объектно-ориентированный JavaScript."
        ),
                "quiz": [
            {
                "q": 'localStorage vs sessionStorage — ключевое отличие?',
                "options": [
                    'localStorage быстрее',
                    'sessionStorage шифрует данные',
                    'sessionStorage очищается при закрытии вкладки; localStorage — до явной очистки',
                    'localStorage доступен только на HTTPS',
                ],
                "correct": 2,
                "wrong_hint": 'sessionStorage — живёт только в сессии вкладки. Закрыл вкладку — данные удалены.',
            },
            {
                "q": "localStorage.setItem('user', {name: 'Alex'}). Что прочитаем через getItem?",
                "options": [
                    "{name: 'Alex'}",
                    'undefined',
                    "'[object Object]' — нужно было JSON.stringify",
                    'null',
                ],
                "correct": 2,
                "wrong_hint": "localStorage хранит только строки. Объект автоматически превращается в '[object Object]'. Используй JSON.stringify/parse.",
            },
        ],
    },

    "M3-L16": {
        "title": "Классы ES6: constructor, методы, наследование",
        "progress_badge": "JS · 16/20",
        "opening": (
            "❓ Зачем классы, если есть обычные объекты?\n"
            "👉 Классы — шаблон для создания множества похожих объектов с общими методами."
        ),
        "hook": "~5 мин: создашь класс с методами и наследованием — это как делают во всех фреймворках.",
        "theory": (
            "<b>Класс:</b>\n"
            "<code>class Animal {\n"
            "  constructor(name, sound) {\n"
            "    this.name = name;\n"
            "    this.sound = sound;\n"
            "  }\n"
            "  speak() {\n"
            "    return `${this.name} говорит ${this.sound}`;\n"
            "  }\n"
            "  static create(name) {\n"
            "    return new Animal(name, '...');\n"
            "  }\n"
            "}</code>\n\n"
            "  • <code>constructor</code> — вызывается при new.\n"
            "  • <code>this</code> — текущий экземпляр.\n"
            "  • <code>static</code> — метод класса (не экземпляра).\n\n"
            "<b>Наследование:</b>\n"
            "<code>class Dog extends Animal {\n"
            "  constructor(name) {\n"
            "    super(name, 'Гав');\n"
            "    this.tricks = [];\n"
            "  }\n"
            "  learn(trick) {\n"
            "    this.tricks.push(trick);\n"
            "  }\n"
            "}</code>\n\n"
            "<b>Приватные поля</b> (современный JS):\n"
            "<code>class Counter {\n"
            "  #count = 0;\n"
            "  increment() { this.#count++; }\n"
            "  get value() { return this.#count; }\n"
            "}</code>"
        ),
        "nuance": (
            "class в JS — синтаксический сахар над прототипами. "
            "Под капотом — prototype chain. "
            "instanceof Animal — проверяет цепочку прототипов."
        ),
        "task": (
            "Класс Task: constructor(title, priority='normal'), "
            "методы complete(), toString() (возвращает строку с [✓] или [ ]), "
            "статический метод fromString(str) для парсинга. "
            "Класс TodoList extends Array: методы addTask(), removeCompleted(), getSorted()."
        ),
        "checklist": [
            "Класс Task с constructor и методами",
            "static метод",
            "Класс-наследник или второй класс",
            "super() в наследнике",
            "Создание экземпляров и тест методов",
        ],
        "hint": "class Task { #done = false; complete() { this.#done = true; } }",
        "quiz_done_line": (
            "Классы в руках — напиши Task и TodoList. Потом зачёт."
        ),
        "win_message": (
            "🏛 Классы — основа ООП в JS и всех современных фреймворков.\n"
            "<b>Ты уже умеешь:</b> создавать классы с наследованием и приватными полями.\n"
            "Дальше — модули: организация кода по файлам."
        ),
                "quiz": [
            {
                "q": 'super() в конструкторе дочернего класса — зачем?',
                "options": [
                    'Опциональный вызов для оптимизации',
                    'Копирует свойства родителя',
                    'Вызывает конструктор родителя — обязателен до использования this',
                    'Вызывает все методы родителя',
                ],
                "correct": 2,
                "wrong_hint": 'Без super() в дочернем классе обращение к this выбросит ReferenceError. Это не опция.',
            },
            {
                "q": 'instanceof проверяет?',
                "options": [
                    'Тип как typeof',
                    'Равенство двух объектов',
                    'Принадлежность объекта к классу или его предкам в цепочке прототипов',
                    'Наличие метода у объекта',
                ],
                "correct": 2,
                "wrong_hint": 'instanceof проверяет прототипную цепочку. [] instanceof Array → true, [] instanceof Object → тоже true.',
            },
        ],
    },

    "M3-L17": {
        "title": "Модули: import, export, динамический импорт",
        "progress_badge": "JS · 17/20",
        "opening": (
            "❓ Как разбить 1000 строк JS на несколько файлов?\n"
            "👉 ES-модули: каждый файл — отдельный модуль с чётким интерфейсом."
        ),
        "hook": "~4 мин: разобьёшь код на модули — так устроен любой React/Vue/Angular проект.",
        "theory": (
            "<b>Экспорт:</b>\n"
            "<code>// utils.js\n"
            "export const PI = 3.14;\n"
            "export function add(a, b) { return a + b; }\n"
            "export default class Calculator { ... }</code>\n\n"
            "<b>Импорт:</b>\n"
            "<code>// main.js\n"
            "import Calculator from './utils.js'; // default\n"
            "import { PI, add } from './utils.js'; // named\n"
            "import * as utils from './utils.js'; // всё</code>\n\n"
            "  • Named export — экспортировать можно несколько.\n"
            "  • Default export — один на файл, импортируется без фигурных скобок.\n\n"
            "<b>Динамический импорт</b> (lazy loading):\n"
            "<code>button.addEventListener('click', async () => {\n"
            "  const { Chart } = await import('./chart.js');\n"
            "  new Chart(...);\n"
            "});</code>\n\n"
            "<b>В HTML:</b>\n"
            "<code>&lt;script type=\"module\" src=\"main.js\"&gt;&lt;/script&gt;</code>\n\n"
            "Модули: строгий режим по умолчанию, собственный scope, загрузка один раз."
        ),
        "nuance": (
            "type=\"module\" в script — CORS: файлы нельзя открыть через file:// напрямую. "
            "Нужен локальный сервер (VS Code Live Server, http-server)."
        ),
        "task": (
            "Раздели проект на 3 файла: api.js (fetchPosts — named export), "
            "render.js (renderList — named export, шаблон), main.js (импортирует оба, запускает). "
            "<code>&lt;script type=\"module\"&gt;</code> в HTML. Локальный сервер или Live Server."
        ),
        "checklist": [
            "api.js с named export функции fetchPosts",
            "render.js с named export renderList",
            "main.js импортирует из обоих файлов",
            "script type module в HTML",
        ],
        "hint": "Используй VS Code расширение Live Server (правая кнопка → Open with Live Server).",
        "quiz_done_line": (
            "Модули в руках — раздели код на файлы. Потом зачёт."
        ),
        "win_message": (
            "📦 Модульный код — стандарт профессиональной разработки.\n"
            "<b>Ты уже умеешь:</b> организовывать код в модули с чётким API.\n"
            "Дальше — обработка ошибок: код становится надёжным."
        ),
                "quiz": [
            {
                "q": 'Разница: export default fn vs export const fn?',
                "options": [
                    'default загружается быстрее',
                    'default — один на файл, импортируется без {}; named — несколько, нужны {}',
                    'named нельзя переименовать при импорте',
                    'default нельзя использовать с Tree Shaking',
                ],
                "correct": 1,
                "wrong_hint": "import fn from './m' (default). import { fn } from './m' (named). Named можно переименовать: import { fn as myFn }.",
            },
            {
                "q": "Динамический import('./module.js') возвращает?",
                "options": [
                    'Модуль синхронно',
                    'undefined',
                    'Promise который резолвится в объект модуля',
                    'Строку с кодом модуля',
                ],
                "correct": 2,
                "wrong_hint": "import() — асинхронный. Используй await import('./module') или .then(m => ...). Нужен для lazy loading.",
            },
        ],
    },

    "M3-L18": {
        "title": "try/catch, Error, кастомные ошибки",
        "progress_badge": "JS · 18/20",
        "opening": (
            "❓ Что происходит с приложением, если запрос к API упал?\n"
            "👉 Падает всё, если нет try/catch. Обработка ошибок — разница между прototипом и продуктом."
        ),
        "hook": "~4 мин: научишься перехватывать и обрабатывать ошибки — код станет устойчивым.",
        "theory": (
            "<b>try/catch/finally:</b>\n"
            "<code>try {\n"
            "  const data = JSON.parse(badJSON);\n"
            "} catch (err) {\n"
            "  console.error(err.name, err.message);\n"
            "} finally {\n"
            "  // выполняется всегда\n"
            "}</code>\n\n"
            "<b>Объект Error:</b>\n"
            "  • <code>err.name</code> — 'TypeError', 'SyntaxError', 'ReferenceError'.\n"
            "  • <code>err.message</code> — описание.\n"
            "  • <code>err.stack</code> — стек вызовов.\n\n"
            "<b>throw:</b> бросить ошибку вручную:\n"
            "<code>if (!user) throw new Error('Пользователь не найден');\n"
            "throw new TypeError('Ожидалась строка');</code>\n\n"
            "<b>Кастомные ошибки:</b>\n"
            "<code>class ValidationError extends Error {\n"
            "  constructor(field, message) {\n"
            "    super(message);\n"
            "    this.name = 'ValidationError';\n"
            "    this.field = field;\n"
            "  }\n"
            "}\n"
            "throw new ValidationError('email', 'Неверный формат');</code>\n\n"
            "<b>Глобальные обработчики:</b>\n"
            "<code>window.addEventListener('unhandledrejection', e => console.error(e.reason));</code>"
        ),
        "nuance": (
            "catch перехватывает всё — и TypeError, и SyntaxError, и твои кастомные. "
            "Используй instanceof для различия: "
            "<code>if (err instanceof ValidationError) { ... }</code>"
        ),
        "task": (
            "Функция parseUser(json): парсит JSON, валидирует наличие name и email (ValidationError), "
            "возвращает объект. try/catch вокруг вызова: разная обработка ValidationError и SyntaxError. "
            "Тест с корректными данными, отсутствием поля и битым JSON."
        ),
        "checklist": [
            "Кастомный класс ValidationError extends Error",
            "throw ValidationError при отсутствии полей",
            "try/catch с instanceof для разных ошибок",
            "Тест трёх сценариев",
        ],
        "hint": "catch (err) { if (err instanceof ValidationError) { ... } else { throw err; } }",
        "quiz_done_line": (
            "Ошибки под контролем — напиши parseUser с тремя сценариями. Потом зачёт."
        ),
        "win_message": (
            "🛡 Обработка ошибок — признак продакшн-кода.\n"
            "<b>Ты уже умеешь:</b> перехватывать, различать и создавать ошибки.\n"
            "Дальше — ещё один практический урок, потом итоговый проект!"
        ),
                "quiz": [
            {
                "q": 'Блок finally выполнится?',
                "options": [
                    'Только при успехе (нет ошибки)',
                    'Только при ошибке',
                    'Всегда — с ошибкой или без',
                    'Только если catch не поймал ошибку',
                ],
                "correct": 2,
                "wrong_hint": 'finally гарантированно выполняется. Используют для cleanup: закрытие соединений, скрытие лоадера.',
            },
            {
                "q": 'Как поймать только TypeError, пропустив другие?',
                "options": [
                    'catch автоматически фильтрует по типу',
                    'try...catch...TypeError синтаксис',
                    'Проверить err instanceof TypeError внутри catch',
                    'Использовать try...catch...else',
                ],
                "correct": 2,
                "wrong_hint": 'catch ловит всё. Проверяй тип внутри: if (err instanceof TypeError) { ... } else { throw err; }',
            },
        ],
    },

    "M3-L19": {
        "title": "Продвинутый JS: замыкания, this, деструктуризация",
        "progress_badge": "JS · 19/20",
        "opening": (
            "❓ Почему в интервью спрашивают про замыкания?\n"
            "👉 Потому что без них непонятно, как работает half дизайн-паттернов в JS."
        ),
        "hook": "~5 мин: разберёшь замыкания, this и продвинутую деструктуризацию — основы для собеседований.",
        "theory": (
            "<b>Замыкания (Closures):</b>\n"
            "Функция «запоминает» переменные из внешней области видимости:\n"
            "<code>function makeCounter() {\n"
            "  let count = 0;\n"
            "  return () => ++count;\n"
            "}\n"
            "const counter = makeCounter();\n"
            "counter(); // 1\n"
            "counter(); // 2</code>\n\n"
            "Применение: приватные данные, фабрики функций, memoization.\n\n"
            "<b>this в JS:</b>\n"
            "  • В методе объекта: <code>this</code> = объект.\n"
            "  • В обычной функции: <code>this</code> = window (или undefined в strict).\n"
            "  • В arrow function: <code>this</code> из внешнего контекста (lexical this).\n"
            "  • <code>bind/call/apply</code> — явная привязка this.\n\n"
            "<b>Продвинутая деструктуризация:</b>\n"
            "<code>// Массив\nconst [first, , third, ...rest] = [1,2,3,4,5];\n\n"
            "// Вложенные объекты\nconst { address: { city, zip } } = user;\n\n"
            "// В параметрах функции\nfunction display({ name, age = 0 }) { ... }</code>"
        ),
        "nuance": (
            "Классическая ловушка: замыкание в цикле с var — все функции видят одно i. "
            "С let — каждая итерация получает свой i. "
            "Это объясняется через block-scope и closure."
        ),
        "task": (
            "Фабрика счётчиков makeCounter(start=0, step=1) — замыкание. "
            "Объект с методами, используй стрелочную функцию где this нужен из внешнего контекста. "
            "Деструктуризация вложенного объекта в параметре функции."
        ),
        "checklist": [
            "makeCounter через замыкание с start и step",
            "Объект с методом, правильный this",
            "Деструктуризация вложенного объекта в параметре",
            "Тесты с console.log",
        ],
        "hint": "function makeCounter(start=0, step=1) { let n = start; return () => (n += step); }",
        "quiz_done_line": (
            "Замыкания и this — тонкие темы. Напиши makeCounter и протестируй. Потом зачёт."
        ),
        "win_message": (
            "🧠 Замыкания и this — темы, которые отделяют джуна от мидла.\n"
            "<b>Ты уже умеешь:</b> работать с замыканиями и контролировать контекст.\n"
            "Осталось — итоговый проект: собираешь полноценное приложение с нуля!"
        ),
                "quiz": [
            {
                "q": 'Что такое замыкание (closure)?',
                "options": [
                    'Синтаксис для объявления функции',
                    'Ключевое слово closure',
                    'Функция сохраняет доступ к переменным внешней области даже после её завершения',
                    'Функция которая вызывает сама себя',
                ],
                "correct": 2,
                "wrong_hint": "Замыкание: функция 'запоминает' лексическое окружение где была создана. Основа для private-переменных.",
            },
            {
                "q": 'Стрелочная функция и this — в чём отличие от обычной?',
                "options": [
                    'Arrow не имеет this вообще',
                    'Arrow this всегда равен window',
                    'Arrow заимствует this из лексического (внешнего) контекста',
                    'Arrow this = объект в котором определена',
                ],
                "correct": 2,
                "wrong_hint": 'Arrow не создаёт свой this. Использует this из места где функция была определена — решает проблему потери this в коллбэках.',
            },
        ],
    },

    "M3-L20": {
        "title": "Итоговый проект: TODO-приложение",
        "progress_badge": "JS · 20/20 🎓",
        "opening": (
            "❓ Как понять, что ты настоящий Junior Frontend Developer?\n"
            "👉 Когда можешь построить работающее приложение с нуля — без туториалов. Сейчас это время."
        ),
        "hook": "~45 мин: собираешь полноценное TODO-приложение — HTML + CSS + JS + localStorage. Это твоё портфолио.",
        "theory": (
            "Финальный проект — полноценное приложение. Критерии Junior:\n\n"
            "<b>HTML:</b> семантическая разметка, правильная форма с label.\n"
            "<b>CSS:</b> современный дизайн, адаптивность, переменные, flex/grid.\n"
            "<b>JS:</b> модульная структура (или хотя бы функции с одной ответственностью), "
            "ES6+ синтаксис, работа с DOM, события, localStorage.\n\n"
            "<b>Функционал приложения:</b>\n"
            "  ✓ Добавление задачи (форма + Enter).\n"
            "  ✓ Отметить выполненной (чекбокс или клик).\n"
            "  ✓ Удаление задачи.\n"
            "  ✓ Фильтры: Все / Активные / Выполненные.\n"
            "  ✓ Счётчик активных задач.\n"
            "  ✓ «Очистить выполненные» кнопка.\n"
            "  ✓ Сохранение в localStorage.\n"
            "  ✓ Пустое состояние (нет задач — покажи сообщение).\n\n"
            "<b>Архитектура:</b>\n"
            "Данные → рендер → события → изменение данных → рендер. "
            "Один источник правды: массив tasks.\n\n"
            "<b>Дополнительно (для 10/10):</b>\n"
            "  • Drag-and-drop для сортировки.\n"
            "  • Редактирование задачи двойным кликом.\n"
            "  • Тёмная тема.\n"
            "  • Анимация появления/исчезновения."
        ),
        "nuance": (
            "Не пытайся сделать всё сразу. Минимальная версия с 7 функциями → потом улучшения. "
            "Работающий простой код лучше сложного незаконченного."
        ),
        "task": (
            "Полноценный TODO с функционалом из теории: добавление, выполнение, удаление, фильтры, "
            "счётчик, очистка выполненных, localStorage, пустое состояние. "
            "Адаптивный дизайн. Разбит на функции (init, render, addTask, removeTask, toggleTask, filterTasks)."
        ),
        "checklist": [
            "Добавление задачи через форму",
            "Отметка выполнения",
            "Удаление задачи",
            "Три фильтра (все/активные/выполненные)",
            "Счётчик активных задач",
            "localStorage сохранение",
            "Адаптивный CSS с переменными",
        ],
        "hint": "Начни с data-функций (addTask, toggleTask), потом render(), потом события. Не наоборот.",
        "quiz_done_line": (
            "Это твой финальный проект — не торопись. Пришли код или поставь ✅ когда готово. "
            "Ты прошёл весь курс — это серьёзное достижение."
        ),
        "win_message": (
            "🎓 Поздравляю! Ты прошёл полный курс от новичка до Junior Frontend!\n\n"
            "<b>Что ты умеешь:</b>\n"
            "✅ HTML — семантика, формы, доступность.\n"
            "✅ CSS — Flexbox, Grid, анимации, адаптивность.\n"
            "✅ JavaScript — DOM, события, асинхронность, классы, модули.\n\n"
            "<b>Следующий шаг:</b>\n"
            "⚡ React — компонентный подход, state, хуки.\n"
            "🌐 Node.js — серверная сторона.\n"
            "🔧 Git и GitHub — версионирование проектов.\n\n"
            "Удачи — ты уже Junior! 🚀"
        ),
                "quiz": [
            {
                "q": 'Лучший способ отправить форму без перезагрузки страницы?',
                "options": [
                    "form action='javascript:void(0)'",
                    'Убрать тег form',
                    'event.preventDefault() + fetch для отправки данных',
                    'window.onbeforeunload = () => false',
                ],
                "correct": 2,
                "wrong_hint": 'preventDefault останавливает стандартную отправку, fetch отправляет данные асинхронно — чистый современный подход.',
            },
            {
                "q": 'После этого курса ты умеешь строить?',
                "options": [
                    'Только статичные HTML-страницы',
                    'Серверную часть на Node.js',
                    'Полноценные интерактивные frontend-приложения: разметка + стили + логика + API',
                    'Мобильные приложения',
                ],
                "correct": 2,
                "wrong_hint": 'Frontend = HTML + CSS + JS. Ты прошёл весь стек: от структуры до async API-запросов.',
            },
        ],
    },

}


def get_lesson(lesson_id: str) -> Dict[str, Any]:
    return LESSONS[lesson_id]


def total_lessons() -> int:
    return len(LESSON_ORDER)


def lesson_id_for_scheduler_index(idx: int) -> str:
    """Индекс урока из scheduler_course → lesson_id каталога."""
    if not LESSON_ORDER:
        return "M1-L1"
    return LESSON_ORDER[idx % len(LESSON_ORDER)]
